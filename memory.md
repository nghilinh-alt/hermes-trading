# Hermes-Trading — Project Memory
_Rogue Night consulting project. Updated at end of each session._

## Last Updated
2026-07-21 (session 21c — **ICT dashboard rebuild DEPLOYED and verified live** (commit `ba23d38`). 196 tests green locally, 180 passed/16 skipped on the VPS, fast-forwarded `a5e52fe..ba23d38`, worker restarted as PID 2542029. All four `context.json` files writing (4.7-6.6 KB), heartbeat carrying equity/circuit-breaker/scan_params, **cycle time 91.7s vs 82-86s baseline** — the context dump costs ~6s against a 900s budget, so no need to hoist the detection context. One false alarm investigated and cleared: `max_concurrent reached (1/1)` on all assets plus a halved available balance turned out to be a **manual limit order Linh placed** — which exposes a real coupling: `busy_count` is unfiltered by symbol, so any manual order anywhere on the account stops the ICT worker trading entirely. Outstanding: run `python dashboard.py` locally to confirm end-to-end rendering; partial-P&L fix still awaits its first real partialled trade. See Handoffs → Session 21c.)

### 2026-07-20 (session 21b recap — **dashboard rebuild BUILT and tested, NOT deployed.** Linh answered the plan's 5 open questions (XRP stays out; archived trades fully retired from view; fix the partial-P&L bug; 5-min poll; alerts in scope) and approved the build. Shipped: new pure `hermes_trading/ict/context.py` (`build_market_context` — bias, dealing range/OTE, FVG/OB/breaker/SR/liquidity near price, qualified **and** near-miss candidates with score + failed gates); per-asset atomic `context.json` dump wired into `run_ict_live.py` after the trading cycle and try/except-isolated so it can never affect trading; extended heartbeat carrying equity/circuit-breaker/live scan_params; full `dashboard.py` rewrite (three-state asset cards, CSS price ladder, proposed scenarios, R-based performance by grade, setup-rejection funnel, browser alerts) with the old dashboard archived. **Partial-P&L bug fixed** — partial fills now record their realised P&L and close-out sums every Bybit closed-pnl leg rather than taking only the first, tagging `pnl_source` when it can't reconcile; closed trades now carry `realised_r`/`planned_rr`/direction-aware `pnl_pct`. 114 tests green + 11 targeted live tests, incl. a parity test asserting the dashboard's armed setups exactly match `scan_asset`'s alerts. **Deploy still gated on Linh's sign-off**; full real-data test suite must be run outside the sandbox first (pre-existing 45s-per-command ceiling, confirmed not a regression). See Handoffs → Session 21b and `dashboard-ict-plan-2026-07-20.md` §0/§11.)

### 2026-07-20 (session 21 recap — **dashboard rebuild PLAN written.** Linh asked to update the dashboard for the new ICT worker, wanting per-asset entry/exit/SL/TP when in a trade, and proposed scenarios + FVG/OB/S&R zones when not. Two findings drove the plan: (1) `dashboard.py` is entirely disconnected from the live system — every path belongs to the halted indicator-weight agent, and ~60% of its UI has no ICT analogue; (2) the "potential trades + zones" data **is not persisted anywhere** — the worker computes it every cycle in `build_detection_context()` and discards it, `sr_zones()` is never called in the live path at all, so the requirement needs a worker-side change. Approved approach: worker writes a per-asset `context.json` (new pure `hermes_trading/ict/context.py`, hooked in after `run_full_cycle` and try/except-wrapped so it can never affect trading), dashboard renders only; qualified **and** near-miss setups with score/20 and failed gates; HTML/CSS price ladder, no chart library. Also flagged two real bugs: `partial_realized_pnl_usd` is never accumulated (partialled trades may record wrong `pnl_usd`), and closed trades carry no R-multiple. **No VPS access this session** (`Network is unreachable`) — plan is derived from source, and its Phase 0 is to verify real on-disk schemas before coding. Deliverable: `dashboard-ict-plan-2026-07-20.md`. See Handoffs → Session 21.)

### 2026-07-18 (session 18 recap — **PIVOT EXECUTED: live worker halted, old system fully archived, ICT Phase 1 built/tested/shipped.** Continuation of session 17's pivot decision. **(A) VPS network access is back** — contrary to every session since ~12 ("this sandbox has no VPS network access"), SSH to `root@187.127.108.173` worked directly from this sandbox this session (key auth via `~/.ssh/hermes_vps`, confirmed banner/whoami/pgrep/file reads). Cause unconfirmed (sandbox networking changed at some point) — don't assume the old limitation still holds; re-verify at the start of a session rather than trusting the old note. **(B) Live worker halted.** Pre-halt safety check: direct `has_open_position()` call against live Bybit for all 5 assets confirmed **flat** (zero open positions) immediately before killing it. `pkill -f hermes_trading.run` on the VPS; confirmed stopped (no process, `bot.log` quiet since). VPS `.venv`/`bot.log`/code left in place untouched — only the process was killed, fully reversible (restart command in the archive README). **(C) Archive consolidated + committed** (commit `61d3af6`): the session-17 flat-YAML snapshot copies were superseded by `git mv`-ing the FULL `state/` tree (all per-asset `strategy.yaml`/`trades.jsonl`/`hypotheses.jsonl`), `state-scalp/`, root `strategy.yaml`, and `patch_strategies.py` into `archive/live-paper-archived-2026-07-18/`; README updated with the halt confirmation. Also committed the accumulated session 13-17 diagnosis/deploy docs and the ICT spec/prompt that were sitting uncommitted. **(D) ICT Phase 1 built, tested, and shipped** (commit `1b6efaa`): `hermes_trading/ict/` (types, structure, liquidity, imbalance, bias) + `hermes_trading/brokers/base.py` (interface only) exactly per `ict-claude-code-prompt.md`'s Phase 1 scope — pure, deterministic, no-lookahead detectors, no exchange calls, no order placement. **66 tests, 96% line coverage**, all green (`pytest tests/ict/`), full package `py_compile` clean. Pushed the new code to the VPS git-pull staging clone (`/opt/trading/hermes-trading/`, code sync only — the running `hermes_trading/hermes_trading/` clone was never touched, nothing activated) and to GitHub (`f3ae306..1b6efaa`). **Resolved spec ambiguities** (flagged, not asked mid-build per Linh's standing "make the reasonable call" instruction): (1) real-data hand-labeling swapped for hand-built synthetic candle fixtures (exact ground truth, fully hermetic) + a seeded pseudo-random stress test, since this session's Python env lacked a working `ccxt`-enabled venv — flagged in `tests/ict/fixtures/README.md` for a real-data pass before backtesting; (2) `brokers/` nested under `hermes_trading/brokers/` (not top-level) for consistency with the existing `adapters/` package and because `pyproject.toml` only packages `hermes_trading`; (3) OTE (0.62-0.79 retracement) direction wasn't pinned in the spec text — implemented as bias-relative (a long's OTE = 21-38% from the range low, a short's = 62-79%, i.e. mirrors); (4) MSS strictly requires the sweep precondition per spec wording — a close beyond the opposing swing with no prior sweep is left unclassified, not treated as MSS. **NOT done this session, by design**: §9 scoring/state machine, §11 backtester, §12 scanner, live/paper worker — Phase 1 stops here per the build prompt, awaiting sign-off before Phase 2.

**(E) Same session, continued — Phase 2 built + real-data validated** (commit `c7a8bc4`). Linh asked "does the strategy have SL/TP/zones" (no — Phase 1 was detection primitives only) then "start phase 2." Built the pieces that turn detected structure into an actual trade plan: `hermes_trading/ict/setup.py` (entry-zone selection, stop/target/RR, session filter, Stage 1 mandatory gates, Stage 2 weighted score + grade — S:6/S:8/S:9), `hermes_trading/ict/risk.py` (position sizing/leverage/circuit breakers — S:7; `position_size()` matches the spec's own worked-example table exactly, including the capped-leverage case), `hermes_trading/ict/backtest.py` (wall-clock-aligned multi-TF resampling, the full S:5 state machine with `state_ttl` + invalidation rules, limit fills, partial/BE/trail management, metrics — S:11). **Real-data validation, not just synthetic**: `pip install ccxt` into the system Python (venv still broken, same fix as last session), fetched 2 years of real 15m OHLCV for BTC/ETH/SOL/TAO via Binance public data (`tools/fetch_ict_backtest_data.py`; CSVs gitignored — regeneratable, not committed) and ran the engine end to end (`tools/run_ict_backtest.py`). **That real run found and fixed 3 genuine bugs no synthetic fixture caught**: (1) entry-zone selection had no bound on how far after the MSS a zone could form — on a 2-year series it would pick a zone from months later at a totally unrelated price (fixed: `max_bars_after_mss`, default 10); (2) FVG/OB mitigation was checked as "ever mitigated across the whole precomputed series" instead of "as of the current bar" — wrongly rejected almost every zone since most levels get retested eventually over 2 years (fixed: mitigation now checked as-of `mss.index`); (3) target selection picked the literal nearest liquidity pool with zero minimum distance, and since every confirmed swing counts as a pool there's always a trivial one a few ticks away (fixed: `min_target_atr_mult`, default 2.0×ATR floor). Also found and fixed a **real performance bug** in already-shipped Phase 1 code: `detect_bos`/`detect_mss` recomputed the full trend from scratch every single bar — fine for unit tests, but 213 seconds on one asset's real 2-year 1H series (17,520 bars). Rewrote with an incremental trend tracker, O(bars × swings) → O(bars + swings log swings), confirmed byte-identical output via the existing test suite plus a new regression test for a same-bar-sweep tie-break edge case the optimization briefly got wrong. **First real-data result** (2yr BTC/ETH/SOL/TAO, spec's locked default parameters, untouched): engine runs cleanly end to end on all 4 assets in seconds; BTC finds 4 fully-qualified A+ setups but 0 fill (the conservative same-bar invalidation-priority rule on ambiguous OHLC bars); ETH/SOL/TAO find 0 qualified setups at all. **Zero closed trades this window — an honest first-pass finding, not a bug**: the mandatory-gate stack (bias + sweep + MSS + entry zone + RR≥2 + kill-zone session + risk) is selective by design, and these are the spec's own untuned §3/§9 defaults; §14 already flags the score weights/thresholds as "prime calibration targets." **Deliberately did NOT loosen parameters to force trades to appear** — that would p-hack the first validation run. Parameter calibration (walk-forward/OOS sweeps) is explicitly the next task, not pre-built blind. 127 tests, 90% coverage on `ict/`+`brokers/`. Pushed to VPS staging clone + GitHub (`c66c260..c7a8bc4`). Handoff: next agent reads this entry; decide with Linh whether to (a) run a parameter calibration pass (walk-forward, widen `state_ttl_bars`/loosen score thresholds/etc. and re-measure — the spec's own suggested next step), or (b) accept the mechanical logic as validated-but-conservative and move to Phase 3 (scanner) once a version of the params produces a believable trade sample.)

### 2026-07-18 (session 17 recap) — **STRATEGIC PIVOT: retiring the indicator-weight system, building a new ICT swing strategy.** Linh directed archiving all current live+paper strategies and building a fresh ICT (Inner Circle Trader) system. **(A) Archived** all strategy configs (root + btc/eth/sol/tao/xrp + old scalp stubs) to `archive/live-paper-archived-2026-07-18/` with a README manifest incl. the VPS `pkill` halt command. Snapshot copies — live worker 2433063 still ticks until Linh runs the halt (zero open positions, so no risk). **(B) Three decisions locked via question:** markets = **phased crypto-now / futures-ready** (Bybit BTC/ETH/SOL/TAO first, broker-abstraction layer so CME ES/NQ/YM/Gold/Oil plug in later); end goal = **full automation**; start point = **lock mechanical spec first**. **(C) Wrote `ict-strategy-plan-2026-07-18.md`** — full build plan + the mechanical spec to freeze: every ICT concept (swing pivots→trend/MSS, S/R clustering, liquidity pools, sweeps, displacement, FVG, OB, premium/discount/OTE) codified with ATR-scaled default params; setup state machine (IDLE→BIAS→LIQUIDITY→SWEEP→MSS→ARMED→IN_TRADE→MANAGING); risk sizing (Linh confirmed same session) = **risk 20% of equity PER TRADE** (the loss if stopped; $1000 equity → $200/trade). Leverage is **derived, not fixed**: `leverage = clamp(ceil(notional/equity), 1, 10x)` where `notional = risk_$ / structural_stop_pct`; isolated margin, 1 concurrent position at this account size; stop always sits well inside liquidation (10x → liq ~8-9% vs ~2% stop). Tight stops (<2%) cap at 10x → under-risk (safe), never over. Circuit breakers −20% daily (1 loss)/−40% weekly (2 losses). This is very aggressive (~10-40x pro norms) — deliberate owner choice. min 2R gate; London+NY kill zones; **qualification model REVISED same session per Linh (was too restrictive): replaced all-TRUE 10-item checklist with mandatory-veto gates (HTF bias [Daily-only OK if Weekly not opposing] + liquidity event + MSS + entry zone [FVG/OB/Breaker] + RR≥2 + session + risk) PLUS a weighted score (max 20): A+ ≥14 → full 20% risk, B 11-13 → half 10% risk, <11 → skip. Added Breaker block as 3rd entry-zone type (failed OB that flips polarity). Target cadence 3-8 trades/month/market for sample size. Linh's pasted model said "Risk ≤1%" — contradicts the 20% set moments earlier; kept 20% (most recent explicit instruction), flagged for confirmation.** A+ checklist; example trades; event-driven no-lookahead backtest framework; scanner schema; `hermes_trading/ict/` + `brokers/` architecture; 6 phases (spec-lock→primitives+tests→backtest→scanner→paper→live→futures). **Explained to Linh how S/R/trend are detected mechanically:** all from confirmed fractal swing pivots (N-bar), trend = HH/HL vs LH/LL sequence, S/R = pivot clustering within 0.15×ATR + reference levels (PDH/PDL/PWH/PWL/equal H&L), pivots confirmed N bars later = no lookahead. **NOTHING BUILT YET — awaiting Linh sign-off on §3 mechanical defaults + Phase 1 go.** Sessions 13-16 zero-trade thread is now moot (that system retired). Handoff: next agent reads `ict-strategy-plan-2026-07-18.md`.)

### 2026-07-18 (session 16 recap) — Linh: "any trading yet, review the last 3 days." **Answer: no — zero trades since the 07-14 guard-reorder deploy; BTC/ETH/SOL have never opened a single live trade.** Two read-only SSH blocks (Linh ran them; sandbox still has no VPS route). Health all good: process 2433063 alive, heartbeats current, reorder confirmed live in running `execution.py` (Guard 2 Soft R:R before the min_tp_pct floor). **Corrected root cause — TWO binding gates split by asset, and the reorder addressed neither at root:** (1) **SOL + XRP die at `min_tp_pct`** — nearest-swing 15m TPs land 0.68–1.37%, an order of magnitude below the 3% floor; soft-R:R extension can't bridge 0.7%→3%. Session 15's "floor near self-defeating on 15m" was right; reorder was a half-measure. (2) **BTC + ETH die upstream at `min_confidence`** — never reach execution, conf stuck 25–43% vs the 50% gate raised in session 10; **confidence never exceeds ~49% anywhere in the window**, so 0.5 looks near-unreachable given current indicator weights. Whole-log skips: No entry 7937, session 3360, ambiguous 1815, trend 1060, SL-too-wide 175, plus a constant (timestamp-less, hence earlier miscounted as 0) Structural-TP-too-thin stream on SOL/XRP. Correction logged: my first pull reported "0 TP-too-thin rejects" — a grep artifact (reject lines carry no date prefix), NOT a real zero. No code/config changed. Wrote `diagnosis-session-16-2026-07-18.md`. **Awaiting Linh (live money):** (A) SOL/XRP TP-floor approach — lower min_tp_pct to ~1.0–1.5% / change TP target selection to next-swing-or-ATR / stand up the isolated scalp worker from session 15; (B) BTC/ETH — diagnose the ~43% confidence ceiling BEFORE moving the gate, then lower min_confidence or rebalance weights. One variable at a time; measure 3–5 days. Prior scalp calls (SMA-200 vs EMA-200, price.py approach, asset basket, sizing floor) still open too.)

### 2026-07-13 (session 15 recap) — Linh asked again "why no trades," then greenlit a new scalping strategy. **(A) Diagnosis re-confirmed:** nothing has deployed since 07-08 (`git log` clean past `f3ae306`), so `min_tp_pct: 3.0%` is still the binding gate. Re-derived from source with two new findings: (1) the guard is near self-defeating on a 15m structural-TP strategy — TP is set to the *nearest* 1h/4h swing level, then rejected if that level is <3% away, which on 15m is what nearby swings are; (2) a **guard-ordering issue** in `execution.py::_structural_sl_tp()` — Guard 2 (hard 3% reject) runs *before* Guard 3 (the soft R:R extension that would widen a thin TP to ~4% when SL≈2%), so trades are killed before the logic that would have saved them runs. Reversing the order recovers most of the 3255 rejected attempts while keeping a real 3% floor on the *final* TP — the cleanest fix. Reflection drift still ruled out (gates at baseline, no asset has hit 5 closed trades). Wrote `diagnosis-session-15-2026-07-13.md`. **(B) New scalping strategy planned:** multi-TF S/R zones + VWAP + 50MA + 200MA, **15m**, run as a **separate isolated worker** (own `STATE_DIR=state-scalp`, own process, own inverted gate set) on several assets — zero impact on the live 4 loops except a shared, regression-tested `price.py` addition (net-new `sma_200` + `mtf_sr_zone`; 200MA needs the fetch limit raised 100→250). Fee reality: current orders are market/taker (~0.11% round-trip), which is why 15m not 1m. Deliverables: `plan-scalp-strategy-2026-07-13.md`, `state-scalp/goal.yaml`, `state-scalp/_scalp-strategy-template.yaml`, `deploy-scalp-step1-2026-07-13.md`. **(C) `min_tp_pct` decision MADE + implemented:** overnight check confirmed another zero-trade night (0 across all 5 assets, PID 2248353 still alive from the 07-03 deploy). Linh chose **reorder the guards**. Implemented in `execution.py::_structural_sl_tp()` — Guard 3 (soft R:R extension) now runs before the `min_tp_pct` floor, so the 3% floor applies to the FINAL (widened) TP. Committed `c78dcf8`; verified via head-to-head unit test (recovers the thin-TP killed case → TP widened to 4.59%; still rejects genuinely thin structure; no regression on good setups). Deploy via **content-based patch script** `tools/patch_guard_reorder.py` (NOT scp — isolates this from the still-undeployed session-12 position-sizing fix in the same file). Deploy doc: `deploy-guard-reorder-2026-07-13.md`. **DEPLOYED to VPS 2026-07-14 22:56 (PID 2433063; reorder confirmed live via grep — Guard 2 = Soft R:R, Guard 3 = Option B floor).** Measurement pending over 3–5 days; baseline `bot.log` line L0=29148 for scoped reject counts. Next gate to watch after: `min_profit_usd: 5.0`. Still awaiting Linh on 4 scalp calls — SMA-200 vs EMA-200, price.py approach, scalp asset basket, scalp sizing floor.)

### 2026-07-08 (session 14 recap) — Linh asked "still no trades" 5 days post the session-12 deploy; ran a multi-round live VPS diagnostic (this sandbox still has no direct VPS network access, but Linh ran the SSH blocks). **Root cause found**: the trend-filter soft-gate fix from session 12 worked exactly as intended (4h-disagree skips: 6, ambiguous-band skips: 59, both tiny over the 4d20h post-deploy window) and reflection-driven gate drift was ruled out (no asset has reached the 5-closed-trade threshold to trigger a single reflection yet — `hypotheses.jsonl` empty for all 5 assets on the live VPS). The actual dominant gate, previously assumed to be SOL-specific, is **`min_tp_pct: 3.0%`** — the structural-TP guard in `execution.py`, which only runs after a signal clears every upstream gate. It rejected 3255 attempts across BTC/ETH/SOL/XRP/TAO over the same window (ETH 1057, BTC 982, SOL 673, TAO 332, XRP 211) — more than the session-time-block itself (2240). Signals ARE being generated in volume; they're dying at the very last step before execution. Documented in `diagnosis-session-14-2026-07-08.md`. **No code or config changed — awaiting Linh's decision** on whether to lower `min_tp_pct` (frequency should recover sharply) or keep it as a deliberate quality floor.)

### 2026-07-06 (session 13 recap) — Linh reported "still no trades," 3 days after the session 12 trend-filter soft-gate deploy, exactly at the deploy doc's own "re-check after 3-5 days" checkpoint. Wrote `diagnosis-session-13-2026-07-06.md` — read-only SSH block: bot up/heartbeats, trade counts since 07-03, whether the soft-discount code is actually the running version, and a skip-reason tally. No code or config changed; diagnostic never run.

### 2026-07-03 (session 12 recap) — two fixes: (1) trade-frequency collapse — daily/4h trend filter AND-gate was hard-skipping BTC/XRP every tick for hours; converted 4h to a soft confidence discount. **Deployed and confirmed live** — 11:35 UTC tick shows all 4 active assets clearing the trend gate into real indicator evaluation. Deploy hit a restart failure (bot down ~1h, see doctrine note) fixed via `setsid`. (2) position sizing bug — leverage was never actually applied to notional/exposure despite session 11's own docstring assuming it was; fixed to `notional = margin × leverage`, plus `position_pct` is now a confidence-scaled floor. Implemented + validated locally; **pending VPS deploy** — hold until trend-filter effect on trade frequency is observed for a few days

## Project Overview
**As of session 20 (2026-07-19/20): the ICT strategy is LIVE WITH REAL MONEY on Bybit.** The old indicator-weight live agent is HALTED and fully archived (session 18). Rebuilt as an ICT (Inner Circle Trader) swing strategy — see `ict-strategy-plan-2026-07-18.md` (spec) and `ict-claude-code-prompt.md` (build prompt/phases). **Linh explicitly chose to skip paper trading (Phase 4) and go straight to live (Phase 5), keeping the spec's locked 20%-of-equity-per-trade risk sizing** — see the Active State entry below for the full context, the safety gaps flagged before building it, and what's still genuinely unvalidated about the underlying edge. The live worker (`tools/run_ict_live.py`) has been running on the VPS since 2026-07-20 ~02:38 UTC, real Bybit account, currently flat on all 4 assets (BTC/ETH/SOL/TAO), $808 USDT balance.
- **VPS**: root@187.127.108.173. **VPS network access status: re-verify each session** — sessions 12-17 all noted "no VPS network access from this sandbox," but session 18 found direct SSH worked fine (key auth via `~/.ssh/hermes_vps`). The limitation may have been sandbox-specific and not permanent; don't assume either way without testing.
- **VPS layout** (confirmed session 5b, 2026-05-27; old system's layout, still relevant for the archive/rollback):
  - **Git pull clone**: `/opt/trading/hermes-trading/` (hyphen) — deploy staging; properly git-tracked as of session 19-20 (was 18 commits stale from ad-hoc tar/scp pushes in earlier sessions bypassing git; fast-forwarded clean to `694c803`, no conflicts since local had no unique commits). **Now also runs the live ICT Phase 3 scanner daemon** — own `.venv/`, started via `setsid`, logs to `scanner.log`, deliberately separate from the halted old system's runtime dir below.
  - **Running agent dir**: `/opt/trading/hermes_trading/` (underscore) — has `.venv/`, `bot.log` at top level, package at `hermes_trading/`, per-asset state at `state/<slug>/`. **Worker process halted session 18** (was PID 2433063) — files/venv/state left in place for rollback, nothing deleted.
  - **State is NOT nested** inside the package dir. It's a sibling: `/opt/trading/hermes_trading/state/<slug>/strategy.yaml` (correct), NOT `/opt/trading/hermes_trading/hermes_trading/state/...` (wrong, this path doesn't exist)
  - **Venv**: `.venv/` (dot-prefixed), activate with `source /opt/trading/hermes_trading/.venv/bin/activate`
  - **Log**: top-level `bot.log` (NOT `logs/hermes.log` — that dir doesn't exist)
  - Fossilized old install at `/opt/trading/` top-level (May-24-dated `strategy.yaml`, `dashboard.py`, `state/`) — unused, safe to clean up later
- **Repo**: https://github.com/nghilinh-alt/hermes-trading.git (master branch)
- **Local path**: `C:\Users\nghil\Projects\Hermes\Hermes-Trading`
- **Local venv is broken in this sandbox**: `.venv/pyvenv.cfg` has `home = /usr/bin` and `.venv/bin/python` is a dangling symlink here (works from a different/prior environment, e.g. a container). Use the system Python (`python`/`python -m pytest`, both on PATH, `ccxt` NOT installed there) or fix the venv before relying on it. This is why ICT test fixtures are synthetic rather than fetched live data (session 18).
- **Old system's trading config** (now archived, not running): BTC/ETH/SOL/XRP USDT-perp, TAO disabled. Full configs at `archive/live-paper-archived-2026-07-18/`.

## Architecture Summary
- **New (ICT, Phase 1 only)**: `hermes_trading/ict/{types,structure,liquidity,imbalance,bias}.py` — pure, deterministic, no-lookahead detection primitives. `hermes_trading/brokers/base.py` — BrokerAdapter interface (no concrete adapter yet). Tests: `tests/ict/` (66 tests, 96% coverage).
- **Old (archived, not running)**: `run.py`/`loop.py`/`adapters/`/`reflect.py`/`dashboard.py` — the retired indicator-weight live agent. Code still in the repo (not deleted, only its state/config archived) in case of rollback, but should not be extended going forward.

## Active State
- **ICT Phase 1**: built, tested (66 passed), pushed to VPS staging clone + GitHub (commit `1b6efaa`).
- **ICT Phase 2**: built (setup/risk/backtest engine), 127 tests, 90% coverage, pushed to VPS staging clone + GitHub (commit `c7a8bc4`). Real 2-year BTC/ETH/SOL/TAO backtest with locked spec defaults produced 0 closed trades.
- **Extended-history validation (session 19)**: Linh asked "are we ready to trade live" — answer was no (unvalidated edge, no paper phase, no execution code, nothing past Phase 2). Linh then asked for the path to live; agreed to extend backtest history + build the Phase 3 scanner in parallel. Re-fetched up to 5yr of data (BTC/ETH back to 2017, SOL to 2020, TAO capped ~2024-04 listing) and reran the calibrated backtest + split-sample check. **Materially better result than the 2yr window**: 154 trades (vs 69), 51.9% win rate, **+$1,490 total PnL** (vs -$1,094), and **all 8 half-window splits across all 4 assets now show positive average R-multiple** (2yr check had every asset with at least one negative half). TAO's 2nd-half PnL still went negative (-$348) despite positive average R -- small sample (7 trades), a few larger losers, worth watching not panicking over. Reading: the 2yr "no edge" finding was substantially a small-sample artifact from an unusually rough 2yr stretch, not a fundamental flaw. **Still not sufficient for live** -- this is one more in-sample backtest pass, not an out-of-sample/paper-forward result -- but meaningfully raises confidence this is worth continuing rather than a dead end.
- **ICT calibration pass** (same session, commit `0d067d3`): Linh asked to loosen parameters for >=2 trades/month/asset. Fixed a real `gate_htf_bias` consistency bug first (was rejecting spec-valid setups), then funnel-traced the real bottleneck (session filter, `swing_n_weekly`, `disp_atr_mult`, `min_rr`, in that order of impact) and loosened accordingly — see `tools/run_ict_backtest.py`'s `CALIBRATED_PARAMS` docstring for the exact set and reasoning. **Result did not hit the target and Linh accepted it anyway, eyes open**: actual filled trades landed at 0.45-0.74/month/asset (short of 2/month even after roughly halving `disp_atr_mult` and `min_rr` from spec defaults), and 3 of 4 assets are net losing at this calibration (BTC profit factor 0.73, ETH 0.54, **SOL 0.06 with a 9.1% win rate and 77.7% max drawdown** — a real red flag, not noise). Only TAO looks solid (PF 1.31, +$522). Linh chose "accept + push" over dialing back toward quality or investigating the ~30% qualified-to-filled conversion rate as an alternative lever.
- **Fill-conversion investigation** (same session, commit `94bb5c8`): Linh asked to pursue the fill-conversion lever after all. Diagnosed the ~2/3 of qualified setups that never fill; found a real bug, not just a loosening opportunity: `_invalidated`'s "MSS retraced fully" check fired on ANY close below `mss.broken_swing.price`, but the entry zone typically sits close to that level by construction, so an ordinary first-attempt retracement bar would often close near (not through) it and get wrongly invalidated on the same bar it touched entry — 25 of 66 "ambiguous same-bar" cases were this graze pattern specifically (confirmed by isolating which of the two invalidation conditions fired). Fixed: the close must now clear the level by `mss_retrace_buffer_mult x ATR` (default 0.25, matching `sl_buffer_mult`'s precedent) to count as a genuine retrace. **Real-data result**: trades 57->69 (+21%), and SOL specifically improved across every metric (PF 0.06->0.45, win rate 9.1%->25.0%, max DD 77.7%->65.5%) — no longer the alarming outlier, though still net losing. Aggregate PnL across all 4 assets did NOT improve (-$923->-$1094) and frequency is still short of 2/month/asset. Pushed to VPS staging + GitHub (`0d067d3..94bb5c8`).
- **Validation pass — split-sample stability check** (same session, commit `053906c`): Linh said "let's go with next tasks" (ambiguous — asked to clarify between finishing validation vs. starting Phase 3 scanner, got no response, proceeded with validation per the project's own "reject if OOS expectancy <=0" guardrail). Added `entry_timestamp` to `ClosedTrade` and `tools/split_sample_check.py`, which splits each asset's trades by entry time into first-half/second-half of the 2yr window. **Finding: none of the 4 assets show consistent performance across both halves** — BTC negative-then-marginally-positive, ETH positive-then-negative, SOL negative-both-halves (worse in 2nd: PF 0.29, WR 11% on 9 trades), TAO strongly-positive-then-net-losing (54% max DD in 2nd half despite a positive average R). Every asset flips sign or degrades meaningfully between halves — the opposite of what a validated edge should look like.
- **SOL root-cause look**: not a uniquely bad market for SOL specifically — ALL 4 assets had large peak-to-trough declines over this window (BTC -61%, ETH -72%, SOL -80%, TAO -81%; this was a rough 2yr stretch for crypto broadly, TAO's raw decline was even worse than SOL's). SOL's 16 calibrated-window trades split 9 bullish (2 wins, -$419) / 7 bearish (2 wins, -$236) — losing in both directions, with the bullish losses clustered Sep-Nov 2025 during what looks like a sustained SOL downleg (a sequence of "buy the dip" entries that kept failing). Read this as **most likely small-sample variance (11-21 trades/asset over 2yr is not a lot) rather than a clean, fixable SOL-specific flaw** — though the pattern is at least consistent with the loosened `swing_n_weekly=2` bias engine being slow to flip from long- to short-bias during a sustained decline. Not conclusively fixable with the data on hand.
- **Net position after all three passes**: this loosened + bug-fixed config is the pushed reference point, but **the validation work argues against calling it a proven edge or moving to Phase 3 (scanner)/any paper-live worker yet**. Every asset's cross-time performance is inconsistent, sample sizes (11-21 trades/asset over 2yr) are too small for statistical confidence either way, and the window itself was an unusually volatile, mostly-declining one for crypto — none of which rules out a real edge, but none of which demonstrates one either. The honest state: **mechanically the engine is correct and well-tested (129 unit tests); the STRATEGY's edge is unproven.**
- **ICT Phase 3 scanner built + deployed live to VPS (session 19-20)**: Linh asked "are we ready to trade live" (no), then for the path to live; agreed to extend backtest history (see extended-history entry above) + build the Phase 3 scanner in parallel, then explicitly asked for it deployed as a running VPS process, not just built locally. Built `hermes_trading/ict/scanner.py` (`Alert` frozen dataclass + `scan_asset()`), mirroring `run_backtest_single_asset`'s per-MSS pipeline but resolving status "as of now" via a new `resolve_setup_status()` added to `backtest.py` (generalizes the existing fixed-lookahead `_search_fill` to an as-of-index version: filled/invalidated/expired/pending). **Locked in with a parity test** (`tests/ict/test_scanner.py`, real BTC data): scanning at bar N must alert on a setup while pending and stop alerting once it resolves, with exact field match (direction, stop, target, rr, grade, mss_level) against what `build_setup` independently computed — the guarantee that live scanning agrees with the already-validated backtest logic rather than silently drifting. Alert-only: no order placement, no position tracking, no API keys (public market data only). Added `tools/fetch_ict_live_data.py` (per-asset rolling, append-only 15m OHLCV cache — seeds 2yr on first run, small incremental fetch thereafter) and `tools/run_ict_scanner.py` (the daemon: 15-min cadence loop across BTC/ETH/SOL/TAO, each asset's cycle wrapped in try/except so one failure can't crash the loop, dedup'd alert log at `state-ict/<asset>/alerts.jsonl` with dedup state persisted across restarts via `state-ict/<asset>/alerted.json`). 132 tests total (129 existing + 3 new scanner tests), all green; the alert-log/dedup write+reload path was directly verified against a constructed `Alert` (not just inferred from the parity test). Found and fixed a real test-performance bug along the way: the scanner parity test initially ran the full detection pipeline against the whole (now up to 5yr) BTC CSV, taking 12 minutes; fixed by capping the test to a 2yr slice + a module-scoped pytest fixture so the expensive search runs once, not per-test (12min → 77s). Commits `ca52502` (scanner core + parity test) and `694c803` (CLI loop + live-data cache), pushed to GitHub.
- **VPS deployment**: discovered the VPS hyphen clone's git history was 18 commits stale — `git status` had misleadingly reported "up to date with origin/master" because no `git fetch` had run since ~session 12; actual local HEAD was `f4746bc`. (Phase 1/2/calibration code had been reaching the VPS via ad-hoc tar/scp pushes in earlier sessions that bypassed git entirely, leaving the working tree ahead of the git history.) Verified local HEAD was a clean ancestor of `origin/master` first, stashed the VPS's local diffs (a redundant dev-deps pyproject.toml addition + the tar-pushed untracked Phase1/2 files, confirmed identical content to what the fast-forward would bring), then fast-forwarded cleanly to `694c803` — zero conflicts. Set up a fresh `.venv` at `/opt/trading/hermes-trading/` (deliberately separate from the halted old system's runtime at `/opt/trading/hermes_trading/`), installed all deps via `pip install -e .` + `pip install pytest`, confirmed `pytest tests/ict/` green on the VPS itself (129 passed, 3 skipped — skips are the scanner parity tests, which need the gitignored local BTC CSV not present on the VPS). Started the daemon via `setsid` (established project pattern), logging to `scanner.log`. **Confirmed stable in production**: 17+ consecutive clean 15-min cycles over several hours (each cycle 88-142s, well inside the 900s budget), 0 errors, 0 alerts yet (expected — base rate ~0.5-0.9 trades/month/asset per the calibration work, so long gaps between alerts are normal, not a sign of malfunction). **Survived a real VPS network-path outage mid-session**: SSH and ICMP both failed from two independent networks (this sandbox AND Linh's own machine) for a period; `uptime` after recovery showed 117 days unbroken, confirming the box itself never went down — it was a network-path/provider issue, not a crash, and since the scanner daemon runs locally and doesn't depend on inbound connectivity, it kept cycling the entire time uninterrupted (verified via unbroken cycle count in `scanner.log` spanning the outage window).
- **LIVE TRADING ACTIVATED (session 20, 2026-07-19/20)**: after the scanner had been running cleanly for a while, Linh said "don't worry about paper trading." Asked two direct scoping questions before building anything real-money: (1) build-and-activate-immediately vs. build-with-a-manual-switch → **Linh chose activate immediately**; (2) keep the locked 20%-risk-per-trade vs. scale down for this first real run → **Linh chose keep the locked 20%**. Both answers logged here plainly since they compound each other on outcome variance, and because the underlying edge is still only in-sample-backtested (see the extended-history + split-sample entries above) — not paper/forward-validated. Given the stakes, used Plan mode (two parallel Explore agents surveying the old system's execution code + the ICT backtest's position-management internals, then a Plan agent to pressure-test the design) before writing any code.
- **Built** (commit `1b85e85`, load_dotenv fix `a5e52fe`): `hermes_trading/brokers/bybit.py` (`BybitBroker(BrokerAdapter)` — auth/idempotent-leverage/order patterns lifted from the archived-but-production-proven `adapters/execution.py`, plus `reduce_position`/`update_trailing_stop`/`set_position_protection`/`fetch_recent_closed_trades`/`set_margin_mode_isolated` beyond the strict interface); `hermes_trading/ict/live.py` (the state machine — reuses `scan_asset`/`resolve_setup_status`/`position_size`/`circuit_breaker_status`/`max_concurrent_ok` exactly as already tested; new `replay_management_bars` mirrors `_manage_position`'s 2R-partial/breakeven/trail thresholds but **never** simulates a stop/target hit from candles — live exits are always ground-truthed from the broker's actual position/closed-pnl state via reconciliation, since a simulated exit would silently diverge from the real fill price/fees; fail-safe by design — local/broker state the reconciliation logic can't confidently explain writes a `NEEDS_MANUAL_REVIEW.flag` and goes inert for that asset rather than guessing; per-asset cycle isolation so one asset's exception can't take down monitoring for the other three). Pulled a pure extraction (`build_detection_context`/`locate_pending_setup`) out of `scanner.py` so `live.py` can re-locate an already-resting order's Sweep/StructureBreak each cycle without duplicating detection logic — no behavior change, `scan_asset`'s own test suite is unchanged. `tools/fetch_ict_live_data_bybit.py`: a Bybit-sourced (not Binance) rolling cache — entries/stops/targets are computed on the same price feed Bybit's own native SL/TP enforce against, deliberately avoiding the cross-exchange basis-risk gap that using the scanner's Binance-sourced cache would have introduced. `tools/run_ict_live.py`: the daemon, hard-fails at startup unless `HERMES_TRADING_MODE=live` + `HERMES_TRADING_I_ACCEPT_RISK=true`, supports `--dry-run` (default off). Deliberately does NOT reuse `_manage_position` directly (that function also owns simulated PnL bookkeeping) — a differential test (`test_replay_management_bars_matches_manage_position_reference`) runs both over the same synthetic bars and asserts agreement, as the regression net against the two drifting apart later. 182 tests total (152 existing + 19 new bybit-broker tests + 30 new live.py tests, including a real-BTC-history walk through the resting-order pending/expired/invalidated/filled transitions), all green.
- **Deployment gap found + fixed mid-deployment**: `tools/run_ict_live.py` never called `load_dotenv()`, so `BYBIT_API_KEY`/`HERMES_TRADING_MODE`/etc were never actually being read from `.env` — `os.getenv()` alone doesn't see it. The old system's `run.py` does this at startup; matched the same pattern. `python-dotenv` was an undeclared transitive dependency `run.py` silently relied on — added explicitly to `pyproject.toml`. Fixed and pushed before the daemon's first real start (`a5e52fe`).
- **VPS deployment**: fast-forwarded `/opt/trading/hermes-trading/` to `1b85e85` then `a5e52fe` (same fetch-first-then-verify-ancestor pattern from the scanner deploy), stopped the running alert-only scanner (`pkill -f run_ict_scanner`, confirmed quiet) since the live worker supersedes it (one process, one state tree, no risk of the two paths silently drifting on different candle sources/params). Copied the OLD system's real Bybit credentials (`BYBIT_API_KEY`/`BYBIT_API_SECRET`, HMAC — no `.pem`/RSA in use) from `/opt/trading/hermes_trading/.env` to the hyphen clone via a plain server-side `cp` + `chmod 600` — never read or typed the actual key values. Verified auth works and pulled the real account balance (**$808.03 USDT**). Pre-flight: confirmed flat via `has_open_position()` on all 4 assets; confirmed one-way position mode (`positionIdx: 0`, not hedge) via Bybit's v5 position-list endpoint; margin mode is account-level `ISOLATED_MARGIN` on this Unified Trading Account (`/v5/account/info` — the authoritative setting; the per-symbol `tradeMode` field reads 0/cross at zero size, which is expected UTA behavior and doesn't override the account-wide isolated setting) — also called `set_margin_mode_isolated()` per-symbol defensively, no errors. Seeded `data/ict-live-bybit/` for all 4 assets (70,080 candles / 730 days each). Started via `setsid`, logging to `live.log`. **Confirmed stable**: 2 consecutive clean cycles (92.2s, 88.0s — well inside the 900s budget), `mode=live dry_run=False`, all 4 assets flat with "no qualified pending setup" (expected — base rate ~0.5-0.9 trades/month/asset), zero errors, zero `NEEDS_MANUAL_REVIEW.flag` files, circuit-breaker state correctly initialized against the real $808 balance.
- **Two things intentionally NOT independently re-verified, flagged for whoever reads this next**: (1) whether Bybit's native `stopLoss`/`takeProfit` order params actually carry through from a *resting limit* order to the resulting position on fill (vs. only market orders, which is the old system's own proven pattern) — docs research + the exact same param shape the old system used successfully for months gave reasonable confidence, and `_handle_resting_order_filled` unconditionally re-applies SL/TP via `set_position_protection` on every resting→filled transition regardless, so a gap here is caught within one 15-min cycle even if the native attach doesn't hold — but this hasn't been empirically confirmed against a real fill yet (there hadn't been one as of session end). (2) the account's actual per-symbol margin-mode enforcement given the UTA tradeMode-vs-account-level ambiguity above — reasoned through, not stress-tested against an adverse-move scenario.
- **Net state — this is real, no longer alert-only**: `tools/run_ict_live.py` is live, unattended, and can place and manage real orders on Bybit for BTC/ETH/SOL/TAO right now. Circuit breakers (-20% daily / -40% weekly), max-concurrent=1 (counting resting orders too, not just filled positions — a deliberate stricter deviation from the literal risk.py signature), and the fail-safe `NEEDS_MANUAL_REVIEW` gate are all live. The underlying strategy's edge is still only in-sample-backtested, not paper/forward-validated — Linh made this call explicitly, twice, with that context in front of them.
- **Old live agent**: halted (session 18), zero open positions confirmed before halt, fully archived at `archive/live-paper-archived-2026-07-18/`.
- VPS running code (old system, halted): `/opt/trading/hermes_trading/hermes_trading/` (nested — package inside package dir). Git repo on VPS: `/opt/trading/hermes-trading/` — properly git-tracked, now runs the live ICT trading daemon (`tools/run_ict_live.py`, real orders) in its own `.venv/`. The alert-only scanner (`tools/run_ict_scanner.py`) is stopped but left in place as a read-only rollback option.

## Key Decisions
| Date       | Decision | Rationale |
|------------|----------|-----------|
| 2026-07-21 (s21c) | `get_balance()` returns `total` (equity), not `free` (available margin) | Both callers — position sizing and the circuit breaker — want equity. With `free`, opening a position was indistinguishable from losing its margin; tripped the breaker at −49.99% on a zero-loss account live. Trade-off accepted: `total` may exclude unrealised PnL, so the breaker measures realised drawdown only |
| 2026-07-21 (s21c) | `max_concurrent` 1 → 4, per-trade risk left at 20%/10% | Linh's call, one slot per traded asset. Shown the ~80%-of-equity-at-risk arithmetic and the fact that entry-time circuit breakers can't prevent it; chose to move one variable and measure. **Risk pairing remains an open decision** |
| 2026-07-21 (s21c) | `busy_count` scoped to the traded asset set, not the whole account | An account-wide count meant any manual order Linh placed silently consumed a worker slot — at max_concurrent=1 it stopped ICT trading entirely (observed live 2026-07-21). Tradeoff: combined manual + worker margin is now unreconciled |
| 2026-07-20 (s21b) | Closed-trade P&L is the SUM of all Bybit closed-pnl legs, not `matching[0]` | A partialled trade produces one record per reduce plus one for the final close; taking the first dropped a leg. Falls back to local estimate + `pnl_source` tag when qty doesn't reconcile, so an unverified figure is never shown as exchange truth |
| 2026-07-20 (s21b) | Performance is measured in R, not % | A+ risks 20% of equity and B risks 10%, so summing raw percentage returns compares different-sized bets. R is the only unit that makes them comparable — and "does A+ actually outperform B" is the question that validates the 20-point scoring model |
| 2026-07-20 (s21b) | Realised R computed off the INITIAL stop, never the trailed stop | Keeps R comparable across trades regardless of how the stop was later managed |
| 2026-07-20 (s21b) | Context dump runs after the trading cycle, try/except-isolated per asset | It is an observability feature; a dashboard data bug must never be able to cost money |
| 2026-07-20 (s21) | Dashboard data comes from a worker-written `context.json`, not local recompute | Dashboard must show what the worker actually saw; a second copy of the detection pipeline would silently drift — the same risk `locate_pending_setup` exists to prevent. Also avoids moving ~70k candles/asset/refresh over SSH |
| 2026-07-20 (s21) | Dashboard shows near-miss setups with score + failed gates, not just qualified ones | At ~0.5-0.9 trades/month/asset, a qualified-only view is blank almost always; *why* setups are rejected is more actionable than the P&L table |
| 2026-07-20 (s21) | Zones rendered as an HTML/CSS price ladder, no chart library | Spatial readability without shipping candle data over SSH or adding a JS dependency |
| 2026-07-20 (s21) | `dashboard.py` to be rewritten in place; old file moved to `archive/` | ~60% of it (indicator bars, reflections, confidence, strategy versions) has no ICT analogue; keeping a dashboard for a halted worker in the project root invites confusion |
| 2026-05-24 | Added per-asset state dirs | Scale to 4 assets independently |
| 2026-05-24 | Reflection reads/writes per-asset memory.md | Hermes agent learns from its own history per asset |
| 2026-05-24 | reflect.py refactored — all paths via --state-dir arg | Fixes silent reflection failure in loop.py |
| 2026-05-24 | dashboard.py uses yaml.safe_load for YAML parsing | Homebrew flat-key parser was missing nested goal values |
| 2026-05-24 | Chose Ollama + qwen2.5:3b over Anthropic API | No API credits needed; VPS has sufficient RAM (7.8GB); CPU inference ~15-20s is acceptable for 5-trade cadence |
| 2026-05-24 | _call_llm uses urllib.request (stdlib) | No extra deps; OpenAI-compatible /v1/chat/completions endpoint |
| 2026-05-24 | VPS running code at nested path hermes_trading/hermes_trading/ | Package installed inside its own package dir — SCP files here, not the top-level dir |
| 2026-05-24 (s2) | loop.py reflection subprocess uses sys.executable | `python` not in PATH on VPS — was silently killing all reflections |
| 2026-05-24 (s2) | reflect.py LLM prompt: explicit variable list + one-shot JSON example | Forces qwen2.5:3b to use indicators[name].field bracket notation correctly |
| 2026-05-24 (s3) | RSI no longer required — min_indicators:2 gates entry instead | Any 2+ indicators firing together is safer than 1 mandatory gate |
| 2026-05-24 (s3) | direction:both — agent evaluates long+short each tick | Higher confidence side wins; ambiguous (<0.1 diff) signals are skipped |
| 2026-05-24 (s3) | _reconcile_open_trades: abandons ALL stale open records | Was leaving ghost "open" trades in trades.jsonl indefinitely |
| 2026-05-24 (s3) | execution.py reads direction from entry_detail | Allows direction:both to place the correct long/short side |
| 2026-05-25 (s4) | Phase 1 SMC: structural SL/TP from support_1h4h/resistance_1h4h | Replaces fixed % SL/TP; SL below swing low + sl_buffer_pct, TP at nearest resistance |
| 2026-05-25 (s4) | Risk-based position sizing: (balance × risk_per_trade) / sl_dist_pct | Replaces fixed 20% of balance; risk_per_trade=10%, capped at MAX_POSITION_USD=$500 |
| 2026-05-25 (s4) | Fixed default_leverage=5 replaces RSI-scaled 3–15x | Simpler; SMC trades sized by risk not leverage |
| 2026-05-25 (s4) | max_sl_pct=5.0% guard: skip trade if structural SL too wide | Prevents entering when S/R level is too far from current price |
| 2026-05-25 (s4) | R:R ratio shown in Live Positions dashboard | Structural TP/SL → real R:R per trade; green ≥1.5R, amber <1.5R |
| 2026-05-27 (s5) | Diagnosed 0% confidence / no-entry symptom — root causes in state/<slug>/strategy.yaml shape, NOT in execution.py | ETH/SOL/TAO YAMLs lack `indicators:` registry → loop falls into long-only RSI<30 fallback. BTC still has rsi.required:true. See diagnosis-session-5.md. |
| 2026-05-27 (s5) | retCode 110043 traced to execution.py:243 only catching ccxt.AuthenticationError | Bybit returns ExchangeError "leverage not modified" when leverage already set; current handler doesn't swallow it. Proposed 7-line idempotent wrapper documented in diagnosis-session-5.md, NOT applied. |
| 2026-05-28 (s6) | $5 hard profit floor: new `_guard_min_profit_usd` in execution.py + `min_profit_usd: 5.0` in all 4 yamls | Linh directive "aim to win at least $5 per trade." Hard skip in `place_live_trade` after qty is computed (qty × \|tp − entry\| < $5 → ValueError). Co-located with Phase 2.1 guards as Guard 4. |
| 2026-05-28 (s6) | reflect.py truncated from 702 → 572 lines (IndentationError fix) | Duplicated `run_hermes` + `main` bodies at lines 572+ from prior Edit-tool corruption. Fixed via Python heredoc; py_compile + ast.parse OK. |
| 2026-05-28 (s6) | `tools/backfill_trades.py` (NEW) — direction-correct pnl_pct via closedPnl/cumEntryValue | Sidesteps the still-broken `(exit-entry)/entry` formula in `fetch_last_closed_pnl` (Phase 2.5 will fix that). Idempotent dedup by order_id. 6 unit tests pass. |
| 2026-06-02 (s7) | Emergency restart after 3-day downtime; no code changes | reflect.py None-safety fix was already on disk (Linh manually cp'd during a partial wave-4 deploy attempt) but agent was pkilled and never relaunched. Restart-only recovery; 8-step deploy block per doctrine #5 worked clean. |
| 2026-06-02 (s7) | Add post-deploy `diff -r hyphen underscore` as standard final check | Doctrine #14 — wave 4 partial cp was invisible for 6 h until reflection needed the missing file. A directory-level diff would have caught it pre-launch. |
| 2026-06-05 (s8) | Raise reflection subprocess timeout 120s → 360s in loop.py | loop.py was killing the reflect.py subprocess before Ollama (configured at 300s) could respond. All reflections silently failed for 3+ days across all 4 assets. |
| 2026-06-06 (s8) | ETH min_confidence raised 0.3 → 0.4 | ETH was entering counter-trend longs at bare-minimum 33% confidence (only RSI + VWAP firing). Indicator fire table showed both RSI and VWAP fire more on losing trades for ETH. |
| 2026-06-06 (s8) | Phase 2.8: old_value sanity check in reflect.py | Before applying LLM mutation, verify stated old_value matches actual current value. TAO v04 Hermes mutation showed LLM misread delta sign but got correct direction by accident. Mismatch flagged as `old_value_mismatch: true` in hypothesis record. |
| 2026-06-06 (s8) | Phase 2.9: tools/audit_historical_pnl.py | Paginated Bybit closed-PnL audit tool. Run on VPS to diagnose -$18k cumRealisedPnl. Supports --weeks, --assets, --csv. |
| 2026-06-06 (s8) | Phase 2.10: version fields added to all 4 strategy.yamls | ETH v03, SOL v01, TAO v04 (matching VPS), BTC v02 already had it. |
| 2026-06-06 (s8) | Phase 2.3: fetch_recent_closed_trades in execution.py | New function for dashboard to pull recent Bybit closed trades directly rather than relying solely on cron backfill. |
| 2026-06-06 (s8) | trail_atr_mult: 2.0 added to all 4 strategy.yamls | Makes the ATR trail multiplier Hermes-tunable. Trail logic was already live in loop.py/execution.py. |
| 2026-06-06 (s9) | Confidence-scaled leverage 3x–10x replaces fixed 5x | execution.py: `leverage = round(min_lev + (max_lev - min_lev) * confidence)`, clipped to [3, 10]. All 4 yamls: default_leverage removed, min_leverage: 3 + max_leverage: 10 added. loop.py _entry_gates_snapshot and reflect.py tunable list updated. |
| 2026-06-08 (s10) | Leverage piecewise curve: 50%→4x, 60%→5x, 75%→7x, 100%→8x | Replaced linear 3x–10x formula. New `_confidence_to_leverage()` in execution.py uses piecewise linear interpolation over `leverage_curve` breakpoints in strategy yaml. Falls back to min/max linear if no curve. |
| 2026-06-08 (s10) | max_trades_per_day reduced to 3 for all assets | Was 10 (or missing on BTC). Caps daily churn per token. |
| 2026-06-08 (s10) | min_confidence raised to 0.5 for all 4 assets | Was 0.3 (BTC/SOL/TAO), 0.4 (ETH). 50% threshold blocks all weak-signal entries, reduces fee bleed from low-conviction trades. |
| 2026-06-08 (s10) | TAO Hermes mutations v03+v04 reverted on VPS | volume_spike min_ratio 2.0→1.5 (v03 based on hallucinated "80% of losers fired" claim — decision_context showed wins had higher volume). bb_squeeze weight 1.25→0.3 (v04 garbled delta sign reasoning). |
| 2026-06-08 (s10) | BTC max_trades_per_day: 10 added | Was missing from BTC yaml; all other assets already had it. |
| 2026-06-18 (s11) | Root cause of June losses: bot traded against daily trend | 155-trade dataset analysis. ETH fell $2117→$1595; bot kept longing. TAO fell $282→$192; bot shorted into +11% bounce. No higher-timeframe filter was in place. |
| 2026-06-18 (s11) | Daily + 4h trend filter: `_get_trend_direction()` in loop.py | Long only when price > daily EMA(20) AND 4h EMA(50) confirms. Short only when price < daily EMA(20) AND 4h EMA(50) confirms. Ambiguous band ±0.3% = skip. Added `ema20_daily` + `ema50_4h` to price.py fetch. All 4 active yamls: `trend_filter.enabled: true`. |
| 2026-06-18 (s11) | Session filter: skip 00:00–07:00 UTC | `session_blocked_end_utc: 7` in loop.py tick. Avoids Asian low-volume session. |
| 2026-06-18 (s11) | Portfolio daily loss cap: -$40 halts all trading for the day | `_portfolio_daily_loss_usd()` sums closed_pnl_usdt across all asset state dirs. June 2 (-$51) and June 7 (-$104) days triggered by 4-asset simultaneous longs into BTC dump. Config: `max_portfolio_daily_loss_usd: 40.0`. |
| 2026-06-18 (s11) | New position sizing: `_position_based_sizing()` in execution.py | `position_notional = balance × 0.10`, `leverage = clamp(risk_pct / (position_pct × sl_dist), 3, 8)`. Replaces old `(balance × 0.10) / sl_dist` formula that was capped at $500 and gave inconsistent risk. New formula: consistent $16 risk (2% of ~$800 balance) regardless of SL distance. All yamls: `position_pct: 0.10`, `risk_per_trade: 0.02`. |
| 2026-06-18 (s11) | TAO disabled: `trading_enabled: false` in strategy.yaml | June performance: -$39 total (-41.3% win rate), largest single-day loss contributor. Replacing with XRP. loop.py now checks this flag and skips entire tick. |
| 2026-06-18 (s11) | XRP added as paper mode replacement for TAO | `state/xrp_usdt/strategy.yaml` created. `trading_mode: paper`. All session 11 config included. After 10+ paper trades with positive win rate → flip to live. |
| 2026-06-18 (s11) | `trading_enabled: false` flag added to loop.py | New YAML flag checked before any tick processing. Allows disabling an asset without stopping its systemd worker. |
| 2026-06-18 (s11) | Windows mount corruption: both Edit AND Write corrupt Python files | Confirmed: only bash heredoc (`cat > path << 'PYEOF'`) works reliably for .py files on C:\Users\nghil mount. Always follow with `python3 -m py_compile`. Markdown/YAML files safe with Edit/Write. |
| 2026-07-03 (s12) | Confirmed root cause via live diagnostic: daily EMA(20) vs 4h EMA(50) disagreement is a sustained multi-hour regime for BTC/XRP, not a brief crossover | Live log showed 100% skip rate on `Trend filter: ambiguous or daily/4h mismatch` across a full 3-hour sample for both assets, same direction each time. The two EMAs' different lookback windows (20 days vs ~8.3 days) disagree far more often/longer than assumed when session 11 designed the AND-gate. |
| 2026-07-03 (s12) | 4h EMA(50) check changed from hard AND-gate to soft confidence discount (`trend_4h_soft_discount: 0.7`) | Daily EMA bias stays a hard gate — that's what actually stopped the June countertrend losses. `_get_trend_direction()` in loop.py now returns `(direction, confirmed)`; caller multiplies confidence by 0.7 when 4h disagrees instead of skipping outright, so strong multi-indicator setups can still clear `min_confidence`. Linh's explicit choice over "drop 4h entirely" or "OR logic" options. |
| 2026-07-03 (s12) | Session 11's "fix empty trades.jsonl + dead cron" action item confirmed still broken | BTC/ETH/SOL `trades.jsonl` are 0 lines on VPS as of this session's diagnostic — 15+ days after being flagged. Hermes reflection has no data for these 3 assets. Needs its own session. |
| 2026-07-03 (s12) | SOL blocked by a different, unrelated gate (`min_tp_pct: 3.0`) — structural TP too thin at current price structure | Not a trend-filter problem; SOL clears trend/confidence/indicator gates fine. Deferred to a separate review — do not loosen `min_tp_pct` as a side effect of the trend-filter fix. |
| 2026-07-03 (s12) | `position_pct` (10%) changed from a fixed value to a floor on MARGIN; scales up with confidence via new `position_pct_curve` | Linh's directive: minimum trade size = 10% of available margin, scaling up on stronger signals. New `_confidence_to_position_pct()` interpolates over `position_pct_curve` (0.5→10%, 0.75→15%, 1.0→20%), clamped to never go below the base `position_pct`. |
| 2026-07-03 (s12) | **Bug fix**: `position_notional` now actually multiplies margin by leverage — previously leverage was set on the exchange via `exchange.set_leverage()` but never applied to `qty`/exposure | Confirmed via the live 2026-06-25 XRP trade: qty×entry = $70.85 notional at 4x leverage on a ~$708 balance — exactly `balance×position_pct`, un-leveraged. But session 11's own docstring worked examples ("$640 notional at 8x leverage" on an $80 base) always assumed leverage scaled notional. New formula: `margin = balance×effective_pct`, `position_notional = margin×leverage`. Verified against all three of session 11's own worked examples (SL 2.5%/4%/5% → $640/$400/$320 notional, $16 risk each) — now exact, previously would have been $80 flat regardless of SL or leverage. **Real trades will be 3–8x bigger notional than they've been for weeks.** Separate deploy doc `deploy-position-sizing-floor-2026-07-03.md`, not bundled with the trend-filter fix, flagged prominently for the magnitude of the change. |
| 2026-07-08 (s14) | **Correction to the s12 row above**: `min_tp_pct: 3.0%` is NOT SOL-specific — it's the single dominant cross-asset skip reason post-trend-fix. 3255 rejections over 4d20h (ETH 1057, BTC 982, SOL 673, TAO 332, XRP 211), more than the session-time-block itself (2240) | Restart-scoped live diagnostic (process running continuously since the 07-03 11:35 deploy, so this is a clean full post-fix window). Trend filter confirmed working (4h-disagree: 6, ambiguous-band: 59 — both tiny). Reflection drift ruled out (`hypotheses.jsonl` empty for all 5 assets — no asset has hit the 5-closed-trade threshold to trigger reflection). This gate — not the trend filter — is now the actual answer to "still no trades." Awaiting Linh's decision on whether to loosen it; not changed yet. |
| 2026-07-13 (s15) | **Guard-ordering finding**: in `_structural_sl_tp()`, Guard 2 (`min_tp_pct` hard reject) runs before Guard 3 (soft R:R extension). Reversing them recovers most of the 3255 rejects while keeping a real 3% floor on the final TP | Guard 3 would widen a thin structural TP to `entry ± sl_dist × min_rr_ratio` ≈ 4% (SL≈2%, RR 2.0) — past the 3% floor — but it never runs because Guard 2 kills the trade first. So part of the "quality floor" is an ordering artifact, not a decision. Cleanest fix for the `min_tp_pct` problem; offered alongside "lower the number" and "keep 3%, widen structural lookback." Still Linh's call, nothing changed. See `diagnosis-session-15-2026-07-13.md`. |
| 2026-07-13 (s15) | **DECIDED + IMPLEMENTED**: Linh chose the guard reorder. `execution.py::_structural_sl_tp()` now runs the R:R extension before the `min_tp_pct` floor. Commit `c78dcf8`; **DEPLOYED 2026-07-14 22:56 (PID 2433063, patch confirmed live)** | The fix for near-zero trades. `min_tp_pct` stays 3.0 — only the order changed, so the floor applies to the final widened TP. Verified head-to-head (thin-TP case recovered to 4.59% TP; genuinely-thin still rejected; good setups unchanged). Deploys via content-based patch `tools/patch_guard_reorder.py` (NOT a whole-file scp — keeps the undeployed session-12 position-sizing fix in the same file out of this deploy = one variable). Doc: `deploy-guard-reorder-2026-07-13.md`. Live-money change; Linh runs the deploy. |
| 2026-07-13 (s15) | New **scalping strategy** approved: multi-TF S/R zones + VWAP + 50MA + 200MA, 15m, as a SEPARATE isolated worker (`STATE_DIR=state-scalp`), several assets, paper first | Linh's choices this session. 15m (not 1m) chosen so taker-fee drag (~0.11% round-trip on market orders) stays tolerable. Isolation = zero impact on the live 4 loops. Only shared surface is `price.py` (net-new `sma_200` + `mtf_sr_zone`, additive; fetch limit 100→250 needs regression test). Gate set fully inverted from live (min_tp_pct 3.0→0.4, min_rr 2.0→1.2, min_profit $5→$1, max_trades/day 3→15, session filter off). Scalp will actually trigger reflection (hits 5 trades fast) → `reflect_bounds` added to cap its tunable ranges. Plan: `plan-scalp-strategy-2026-07-13.md`; step-1 deploy: `deploy-scalp-step1-2026-07-13.md`. Nothing built/deployed yet. |

## Known Issues / TODOs
- **New scripts must call `load_dotenv()` themselves — `os.getenv()` alone will not see `.env`.** Bit us deploying `tools/run_ict_live.py` (session 20): `BYBIT_API_KEY`/`HERMES_TRADING_MODE`/etc all read as empty until `load_dotenv()` was added, matching `hermes_trading/run.py`'s existing pattern. `python-dotenv` itself was an undeclared transitive dependency `run.py` silently relied on for months — now explicit in `pyproject.toml`. Any new entry-point script that reads env-based config/credentials needs this.
- **`git status` "up to date with origin/master" can lie if `git fetch` hasn't run recently** — bit us in session 19-20: the VPS hyphen clone (`/opt/trading/hermes-trading/`) was actually 18 commits behind, but its cached ref made it look current until an explicit `git fetch` was run. Always `git fetch` before trusting a remote clone's git status, especially on a clone that's had code pushed to it via non-git means (tar/scp) in the past.
- VPS running code at `/opt/trading/hermes_trading/hermes_trading/` (nested) — when deploying new code, SCP to this path OR copy from `/opt/trading/hermes-trading/` after git pull
- state/strategy.yaml in root is legacy — per-asset dirs under state/{asset_slug}/ are the source of truth on VPS
- VPS has stale state/hypotheses.jsonl and state/trades.jsonl at root level (old pre-per-asset structure) — safe to ignore
- **`dashboard.py` is dead code against the current system** (found session 21) — every path it reads belongs to the halted indicator-weight agent (wrong VPS dir, wrong log file, XRP instead of TAO, `state/<slug>/` instead of `state-ict-live/<ASSET>/`). Do not debug it as if it were merely stale; rebuild per `dashboard-ict-plan-2026-07-20.md`.
- **[FIXED session 21b] `partial_realized_pnl_usd` was never accumulated** — the `partial_take` branch now records it from the broker's reported fill, and `_handle_position_closed` sums ALL closed-pnl legs (preferring exchange truth, tagging `pnl_source`). **Still verify against the first real partialled live trade** — the fix is tested against a FakeBroker, not against Bybit's actual multi-record behaviour.
- **`sr_zones()` is not called in the TRADING path** (session 21) — only `liquidity_pools()` runs in `build_detection_context`. As of 21b `hermes_trading/ict/context.py` calls it for display, but it still has no influence on any trading decision.
- **Do not use `git stash` on the Windows mount** (session 21b) — `pop` fails with `Operation not permitted` and silently leaves the file at its pre-stash state. Copy files aside instead. Recover a lost stash with `git show 'stash@{0}:path' > path`.
- **Long-running jobs in the Cowork sandbox: unreliable, don't depend on them** (session 21b, corrected twice). A plain `nohup ... &` does NOT survive between bash calls. `setsid nohup ... </dev/null & disown` writing into the mounted folder DOES survive — verified, a pytest run kept going across three separate calls. BUT the launching call itself then tends to be killed (exit 143/137), and on some attempts the redirect target was never created even though the process was running. Net: fine for a best-effort attempt, **not** a dependable way to run a >45s suite. Anything that must be trusted (full `pytest tests/ict/`) belongs on Linh's machine or the VPS.
- **No VPS route and no SSH keys from the Cowork sandbox** (session 21b) — `ssh 187.127.108.173` gives `Network is unreachable`, `~/.ssh` does not exist, and outbound curl to api.bybit.com returns 000. **GitHub IS reachable** (`git ls-remote origin` works), so pushes are possible from the sandbox but nothing VPS- or exchange-facing is. Re-verify each session rather than assuming either way.
- Dashboard requires SSH key auth to VPS; shows last-known-state banner when unreachable
- GitHub SSH key on VPS not yet set up — snapshot.sh push fails silently; run setup_github_ssh.sh then add public key to GitHub
- [FIXED session 2] LLM bracket notation: prompt now has explicit newline-delimited variable list + one-shot JSON example; `python` → `sys.executable` in reflection subprocess

## Handoffs

### Session 21c (2026-07-21) — DEPLOYED to VPS, verified live
- **Deployed** commit `ba23d38` ("feat: ICT dashboard rebuild + market-context dump; fix partial-take P&L accounting"). Full local suite green first: **196 passed in 271s** on Linh's Windows machine (the sandbox could never finish `test_scanner`'s 2-year fixture or the real-CSV half of `test_live`). On the VPS after fast-forward `a5e52fe..ba23d38`: **180 passed, 16 skipped in 2.1s** (skips = real-CSV tests, gitignored data not present — expected).
- **Rollback point was `a5e52fe`**, NOT `692e625` — the VPS clone had never received the docs-only session-20 memory commit. Always read the VPS's own HEAD rather than assuming it matches local.
- **Restart**: backed up `live.log` first (the launch command uses `>`, which truncates) to `live.log.pre-dashboard-20260721_082723`. Killed PID 2522692, restarted as PID 2542029 with the **exact** form already in use — `setsid .venv/bin/python tools/run_ict_live.py > live.log 2>&1 < /dev/null & disown` (direct path, NOT `-m`; no `source .env` needed since `run_ict_live` calls `load_dotenv()` itself). Booted clean: `mode=live dry_run=False`, params intact.
- **Verified live**: all four `context.json` files written (4.7-6.6 KB each — matches the 4-5 KB predicted from local real-data runs), `heartbeat.json` carries all new keys (`equity_usd`, `busy_count`, `circuit_breaker`, `scan_params`, `cycle_seconds`, `dry_run`, `assets`). **Cycle time 91.7s vs the 82-86s baseline — +6s for the context dump, comfortably inside the 900s budget**, so the "detection context builds twice per flat asset" concern is measured and fine; no need to hoist it.
- **False alarm worth recording (and the real lesson underneath)**: the first post-deploy cycle logged `max_concurrent reached (1/1) -- skipping new-entry check` on all four assets, and heartbeat equity read $404.04 against the $808.03 seen in pre-flight 12 minutes earlier. I flagged it as a possible unmanaged position before touching anything. **Cause: Linh had manually placed a limit order on the account.** Nothing wrong with the deploy. **The durable lesson: `run_full_cycle` computes `busy_count = len(get_positions()) + len(get_open_orders())` with NO symbol filter, so ANY manual order or position anywhere on the Bybit account saturates `max_concurrent=1` and silently stops the ICT worker taking any trade at all.** The worker and Linh's manual trading share one slot. Worth deciding whether that's intended — if not, the fix is to filter `busy_count` to the ICT asset set.
- **Also note**: `get_balance()` returns *available* balance, so it drops when margin is locked by an open position. It is not account equity, and the dashboard's "equity" card inherits that meaning.
- **Two follow-up changes made same session, on Linh's instruction (not yet deployed at time of writing)**:
  1. **`DEFAULT_MAX_CONCURRENT_TRADES` 1 → 4** (`hermes_trading/ict/risk.py`) — "max_concurrent can be 4 max", one slot per traded asset. **Per-trade risk deliberately left unchanged at 20% (A+) / 10% (B)**: Linh was shown the arithmetic (4 concurrent A+ = ~80% of equity at risk simultaneously, and the −20% daily / −40% weekly circuit breakers CANNOT prevent it because they're evaluated at entry time in `_look_for_new_setup`, so all four can open before any loss registers) and chose "change max_concurrent only, decide risk later" per the project's one-variable-at-a-time doctrine. **The risk pairing is an open decision, not a settled one** — revisit once there's a real sample of multi-position days. Comment block in `risk.py` carries the same warning.
  2. **`busy_count` now scoped to the traded asset set** (`run_full_cycle`, `hermes_trading/ict/live.py`) — was `len(get_positions()) + len(get_open_orders())` account-wide, now summed per-asset over `assets` via the adapter's existing symbol filtering (kept per-asset rather than matching ccxt's `"BTC/USDT:USDT"` format in this layer, which would put exchange-format knowledge on the wrong side of the BrokerAdapter seam). Accepted tradeoff: total account exposure is now the worker's slots PLUS anything held manually, with nothing reconciling the combined margin. New regression test `test_busy_count_ignores_positions_outside_the_traded_asset_set`.
- **REAL BUG FOUND AND FIXED — `get_balance()` returned FREE margin, not equity** (`hermes_trading/brokers/bybit.py`). It was `usdt.get("free", usdt.get("total", 0))`. Both callers (position sizing AND the circuit breaker) want equity, so merely *opening* a position was indistinguishable from losing the margin it locked. **Observed live 2026-07-21**: Linh's manual order locked $403.99 of an $808.03 account; `free` read $404.04 against a persisted `equity_at_day_start` of $808.03, so the breaker computed **−49.99% and tripped its −20% limit having lost nothing**, standing all four assets down. Confirmed against the API: `free 404.04 / used 403.99 / total 808.03`. **This bug was fully masked by `max_concurrent=1`** — `_look_for_new_setup` checks max_concurrent BEFORE the breaker, so with busy_count=1 it always returned early and the breaker was never evaluated; raising to 4 exposed it on the very next cycle. **It is self-inflicted at scale, not just a manual-order problem**: `position_size` can take notional up to `equity × lev_max`, so the worker opening ONE of its own A+ trades could collapse `free` and stand the account down for the rest of the UTC day — meaning max_concurrent=4 would have delivered one trade then silence, strictly worse than before. Fixed to prefer `total`, with an explicit None-check rather than `or 0` (a present-but-null `total` would otherwise report zero equity, which reads as a −100% day and silently disables trading). 3 regression tests in `test_brokers_bybit.py`, incl. the exact live numbers. **Caveat recorded in the docstring**: `total` is wallet balance and may exclude unrealised PnL, so the breaker now measures REALISED drawdown — appropriate given exchange-native stops and the spec's "−20% daily = 1 loss" framing, but an open underwater position won't trip it before its stop does.
- **IMPORTANT CALIBRATION CAVEAT**: `run_backtest_single_asset` structurally enforces ONE concurrent position (`busy_until_index`, and its own `max_concurrent` default is still 1). **Every calibrated parameter — `min_rr=0.8`, `b_threshold=9`, `state_ttl_bars=40`, etc. — was measured under single-position conditions.** Live now runs 4. There is no backtest evidence for multi-position behaviour; treat early multi-position results as out-of-sample.
- **Outstanding**: deploy the max_concurrent=4 + busy_count changes (worker restart required). Run the dashboard locally (`python dashboard.py`) to confirm end-to-end rendering against real VPS state — not yet done at time of writing. And the partial-P&L fix is still only FakeBroker-verified; the first real partialled trade is the test (see session 21b entry).

### Session 21b (2026-07-20) — Dashboard rebuild BUILT (not deployed)
- **Trigger**: Linh answered the plan's 5 open questions and said "ready to start". Answers: (1) leave XRP out; (2) fully retire archived old-system trades from view; (3) fix the partial-realized-pnl bug; (4) 5-min dashboard poll; (5) alerts in scope.
- **Built**: `hermes_trading/ict/context.py` (NEW — pure `build_market_context()`: bias, dealing range/OTE, FVG/OB/breaker/SR/liquidity near price, qualified **and** near-miss candidates with score + failed gates, gate summary; no I/O, no broker calls). `tools/run_ict_live.py` — per-asset atomic `context.json` dump after `run_full_cycle`, try/except-isolated per asset, plus heartbeat extended with equity/busy_count/circuit_breaker/live `scan_params`/cycle_seconds. `dashboard.py` — full rewrite for ICT (three-state asset cards: open_position / resting_order / flat; CSS price ladder; proposed scenarios with score and human-readable failed gates; R-based performance incl. by-grade breakdown; setup-rejection funnel; browser alerts via localStorage dedup; 5-min poll). Old dashboard `git mv`'d to `archive/dashboard-indicator-weight-2026-07-20.py`.
- **Partial-P&L bug FIXED** (`hermes_trading/ict/live.py`): the `partial_take` branch now accumulates `partial_realized_pnl_usd` from the broker's reported average fill (falling back to the 2R trigger level when the adapter reports none), and records `partial_qty`/`partial_exit_price`. `_handle_position_closed` no longer takes `matching[0]` — it **sums every closed-pnl record** belonging to the position (a partialled trade produces one per reduce plus one for the final close) and prefers that fee-inclusive exchange truth whenever summed qty reconciles against `qty_total` within 1%; otherwise it falls back to newest-record + local estimate and tags the trade `pnl_source: "local_estimate"` so the dashboard flags it rather than presenting it as truth. Exit price is now qty-weighted across legs.
- **Closed trades now record R**: `realised_r`, `planned_rr`, `pnl_pct` (direction-aware — the old system's sign-flip bug is explicitly tested against), `risk_usd`, `pnl_source`, `legs`, `final_stop_price`. R is computed off the INITIAL stop so it stays comparable regardless of later trailing.
- **Tests**: `tests/ict/test_context.py` (NEW, 8 tests) — the load-bearing one is `test_qualified_candidates_match_scan_asset`, asserting set-equality between context-builder armed setups and `scan_asset`'s alerts with exact entry/stop/target/rr/grade/score match, so the dashboard can never disagree with the trading path about what a live setup is. Plus no-lookahead, JSON-size bounds, zones-near-price, near-misses-have-reasons. `tests/ict/test_live.py` +6 tests for the partial fix (including a slipped-fill case and the leg-reconciliation fallback). **114 tests green in 3.9s** across detection/setup/risk/context/no-lookahead/smoke, plus 11 targeted live tests.
- **Real-data validation**: context builder run against BTC/ETH/SOL/TAO — 4-5 KB per asset (after collapsing sweeps, which were 242 raw in a 40-bar window since every confirmed swing is a pool), 0.2s on 8k candles / ~3s on 30k. Recorded a real fixture at `tests/ict/fixtures/context_btc_near_miss.json`: a **score 18/20, grade A+ setup blocked solely by the `rr` gate at 0.78R vs the 0.8 minimum** — exactly the near-miss diagnostic Linh asked for, and previously invisible.
- **NOT deployed.** Deploy gated on Linh's sign-off. **Pre-deploy requirement**: run the full `pytest tests/ict/` somewhere without a 45s per-command ceiling — `test_scanner.py`, `test_backtest.py` and the real-CSV half of `test_live.py` could not complete in this sandbox. **Confirmed pre-existing**, not a regression: stashing all session changes and re-running the untouched tree produced the identical timeout.
- **Sandbox gotchas hit this session (worth not relearning)**: (1) background/`nohup` processes do NOT survive between bash calls — each call is a fresh sandbox, so long jobs must fit in one call or write to the mounted folder; (2) `git stash push` + `pop` on the Windows mount **failed to restore** (`Operation not permitted` unlinking files) and silently left the working file at the pre-stash version — recovered via `git show 'stash@{0}:path' > file`. Avoid stash on this mount; copy files aside instead. (3) A stale `.git/index.lock` was created and could not be removed from the sandbox — **Linh needs to delete it** before any git operation, and drop the leftover `stash@{0}`.
- **Commit hygiene note**: `hermes_trading/ict/backtest.py`, `snapshot.sh`, `tests/ict/test_backtest.py`, `tools/dedup_trades.py` and an archived `goal.yaml` show as modified with equal insertion/deletion counts — **pure CRLF churn, zero content change**, confirmed with `git diff --ignore-cr-at-eol`. Commit with explicit paths so they stay out.
- **Deploy runbook written**: `deploy-dashboard-ict-2026-07-20.md` — step-by-step for Linh to run, because **SSH was `Network is unreachable` from the sandbox all session**. Ordered so nothing touches the live worker until the safety checks pass: clear the stale git lock + run the full local suite → commit with explicit paths (CRLF churn excluded) → VPS read-only pre-flight (no review flags, all four assets `flat` locally AND `has_open_position()==False` at the broker) → `git fetch` before trusting status, `merge --ff-only` → restart using the *exact* command line captured from `pgrep` rather than a guessed invocation → verify one clean cycle wrote four `context.json` files and an extended heartbeat → run the dashboard locally. Includes rollback and the post-deploy watch items.
- **Extra verification this session**: `test_backtest.py` 23 passed (fast, previously assumed slow — the real bottleneck is only `test_scanner.py`'s 2-year fixture and the real-CSV half of `test_live.py`). pyflakes clean across all new/changed files after fixing an unused import, an unused local, and a placeholder-less f-string. Dashboard re-rendered end-to-end post-lint against all three card states.
- **Handoff**: deploy agent. Read `deploy-dashboard-ict-2026-07-20.md` first (it is the executable plan), then `dashboard-ict-plan-2026-07-20.md` §0 (build status + verification) and §11. Run the full suite first, then verify the real on-disk state schemas on the VPS (SSH was unreachable from the sandbox all session, so schemas remain source-derived), then deploy per §9 — flat-check all 4 assets, `git fetch` before trusting the clone, restart, confirm one clean cycle writes four `context.json` files. Watch cycle duration on the first live cycle: a flat asset now builds the detection context twice (once for trading, once for display).

### Session 21 (2026-07-20) — Dashboard rebuild plan for the ICT worker (plan phase)
- **Trigger**: Linh: "Since we have a new ICT Trading worker, we need to update the Dashboard accordingly. Review the current dashboard and devise a plan of improvements to match the ICT Strategy." Specifically wants, per asset: (a) if in a trade — entry/exit/SL/TP zones; (b) if not in a trade — proposed scenario with entry/exit/SL/TP **plus** FVG, Order Blocks, and Support/Resistance zones.
- **Deliverable**: `dashboard-ict-plan-2026-07-20.md`. **No code written, no deploy.** Awaiting Linh's sign-off before implementation.
- **Headline finding**: `dashboard.py` is not stale — it is **entirely disconnected**. Every path it reads belongs to the halted indicator-weight agent: wrong VPS dir (`hermes_trading/` underscore vs the ICT worker's `hermes-trading/` hyphen), wrong log (`logs/hermes.log`, which memory.md already flags as non-existent — ICT logs to `live.log`), wrong asset set (XRP, which ICT doesn't trade; missing TAO, which it does), wrong state layout (`state/<slug>/` vs `state-ict-live/<ASSET>/`), and ~60% of its UI (indicator weight bars, reflection cards, confidence column, signals-fired chips, strategy versions) has no ICT analogue at all and should be deleted rather than ported.
- **Second finding — the blocker for Linh's requirement (b)**: the "potential trades / FVG / OB / S&R" data **is not persisted anywhere**. The worker computes it all every cycle in `build_detection_context()` then discards it: `scan_asset()` `continue`s past any candidate where `not setup.qualified`, throwing away the `TradeSetup` carrying score/grade/gate_failures/entry/stop/target/rr. The cycle's only surviving output is `CycleResult.action`, a one-line string. Also: **`sr_zones()` is never called anywhere in the live path** (only `liquidity_pools()` is), and bias/dealing-range only exist inside `scan_asset`'s per-MSS loop, so a quiet asset has no bias value at all. Requirement (b) therefore needs a worker change, not just dashboard work.
- **Four decisions locked with Linh via question**: (1) data source = **worker writes `context.json` per asset per cycle**, dashboard renders only — rejected local recompute because it would duplicate the detection pipeline and risk showing zones the worker never saw, the exact drift `locate_pending_setup` exists to prevent; (2) setup scope = **qualified AND near-misses** with score/20 and failed gates shown — qualified-only would be blank almost always at a ~0.5-0.9 trades/month/asset base rate; (3) visualisation = **HTML/CSS price ladder + tables**, no chart library, no candle transfer over SSH; (4) this session = **plan only**.
- **Planned architecture**: new pure module `hermes_trading/ict/context.py` → `build_market_context()`, reusing the same `build_detection_context()` call the trading path makes (so the dashboard shows what the worker actually saw), plus `sr_zones()`/`compute_bias()`/`dealing_range()` which the live path doesn't currently compute. Hooked into `run_ict_live.run_once()` **after** `run_full_cycle()` returns, atomic-write, wrapped in try/except — a dashboard data bug must never cost money. Zones bounded to ±3×ATR of price (unmitigated only), pools/SR to ±5×ATR, candidates to the same 61-bar lookback `scan_asset` already uses → a few KB per asset.
- **Bugs found while reviewing (flagged, not fixed)**: (1) **`partial_realized_pnl_usd` is never accumulated** — `_manage_open_position`'s `partial_take` branch reduces `qty_remaining` and sets `partial_taken=True` but never records the partial's realised P&L, so `_handle_position_closed`'s `closed_pnl_usd + partial_realized_pnl_usd` always adds zero; if Bybit logs the 2R partial as its own closed-pnl record, `matching[0]` may pick either leg and a partialled trade's recorded `pnl_usd` will be wrong. **Recommend fixing before the dashboard**, else the dashboard faithfully displays a wrong number. (2) Closed trades carry **no `pnl_pct` and no R-multiple** — for an R-based strategy realised R is the most important number and can't be reliably reconstructed downstream once partials are involved; add at write time. (3) `SCAN_PARAMS` is triplicated by hand across `run_ict_backtest.py`/`run_ict_scanner.py`/`run_ict_live.py` with only a comment enforcing sync — plan has the dashboard read them from the heartbeat so drift becomes visible.
- **VPS access this session: NO.** `ssh root@187.127.108.173` → `Network is unreachable` from the sandbox. Consistent with the standing note that reachability is intermittent and must be re-verified per session. **Everything in the plan is derived from source code, not live inspection** — Phase 0 of the plan is explicitly "verify reachability and snapshot the real on-disk shape of `position.json`/`trades.jsonl`/`heartbeat.json` before coding against assumed schemas."
- **Open questions for Linh** (in §10 of the plan): XRP dropped from the ICT universe — intentional? Should archived old-system trade history stay visible? Fix the partial-P&L bug as part of this work? Drop the dashboard poll from 60s to 5min (worker only cycles every 15min)? Push alerts on arm/fill — in scope or later?
- **Handoff**: implementation agent, once signed off. Read `dashboard-ict-plan-2026-07-20.md`, then this entry, then `ict-strategy-plan-2026-07-18.md` §3 (FVG/OB/S&R mechanical definitions — needed to label the ladder correctly) and §9 (gate + scoring model the dashboard renders). Start at Phase 0. Plan phases 3-5 (dashboard UI) can be built offline against a fixture `context.json`, so they are not blocked on VPS reachability. **Do not restart the live worker without Linh's explicit sign-off — real money, $808.03 as of session 20; confirm flat on all 4 assets first, as session 18 did.**

### Session 18 (2026-07-18) — PIVOT EXECUTED: worker halted, archive committed, ICT Phase 1 shipped
- **Trigger**: Linh: "Read ict-claude-code-prompt then plan out the work. Remember I want to archive all current trading strategies and implement this new ICT strategy. Push to VPS, then github when ready." Continuation of session 17's pivot decision (nothing had actually been executed yet — worker still running, archive uncommitted, ICT unbuilt).
- **Plan mode used** (user explicitly asked to "plan out the work") — plan approved after two clarifying questions: (1) halt the live worker now vs. later → **now**; (2) what "push to VPS, then GitHub when ready" means given Phase 1 has no execution path → **push once tests are green**, since it's inert code.
- **VPS access discovery**: unlike every session since ~12, direct SSH from this sandbox worked. Ran a read-only pre-halt check (`has_open_position()` for all 5 assets via the VPS's own ccxt/dotenv setup — confirmed **flat** on all), then `pkill -f hermes_trading.run`, confirmed stopped (no process, `bot.log` quiet). VPS files untouched otherwise, fully reversible.
- **Archive commit** (`61d3af6`): consolidated the session-17 flat-YAML snapshots into full `git mv`'d trees (`state/`, `state-scalp/`, root `strategy.yaml`, `patch_strategies.py`) under `archive/live-paper-archived-2026-07-18/`; README updated with halt confirmation + rollback command. Also swept up session 13-17's uncommitted docs.
- **ICT Phase 1 commit** (`1b6efaa`): `hermes_trading/ict/` (structure/liquidity/imbalance/bias/types) + `hermes_trading/brokers/base.py`, built exactly to `ict-claude-code-prompt.md`'s Phase 1 scope. 66 tests, 96% coverage, all green; full-package `py_compile` clean. Pushed to the VPS hyphen staging clone (code sync only, running clone untouched, nothing activated) and to GitHub.
- **Not done Phase 1, by design**: Phase 2 (backtester, §11), Phase 3 (scanner, §12), any live/paper worker, any order placement. The build prompt is explicit that Phase 1 stops for sign-off before continuing.
- **Same session, continued — Phase 2 built** (commit `c7a8bc4`): Linh asked "does it have SL/TP/zones" then "start phase 2." Added `setup.py` (entry-zone selection, stop/target/RR, gates, scoring — S:6/S:8/S:9), `risk.py` (sizing/leverage/circuit breakers — S:7, matches the spec's worked-example table exactly), `backtest.py` (multi-TF resample, S:5 state machine, fills, management, metrics — S:11). 127 tests, 90% coverage.
- **Real-data validation this session** (not deferred): `pip install ccxt` into system Python, fetched 2 years of real BTC/ETH/SOL/TAO 15m OHLCV (`tools/fetch_ict_backtest_data.py`, CSVs gitignored/regeneratable), ran the engine end to end (`tools/run_ict_backtest.py`). Found + fixed 3 real bugs synthetic tests missed (entry-zone unbounded-future selection, mitigation-ever vs mitigation-as-of-bar, target-with-no-minimum-distance) plus a genuine O(n²) performance bug in already-shipped Phase 1 `detect_bos`/`detect_mss` (213s → 0.0s on one asset's real history; fixed with a regression test for a tie-break edge case the fix briefly introduced).
- **Result**: engine runs cleanly on all 4 real assets; BTC found 4 A+-qualified setups but 0 filled, ETH/SOL/TAO found 0 qualified. Zero trades with the spec's own locked/untuned defaults — reported honestly as a first-pass finding, not patched by loosening parameters to force trades to appear (that would p-hack the validation run). Pushed to VPS staging + GitHub (`c66c260..c7a8bc4`).
- **Same session, continued again — calibration pass** (commit `0d067d3`): Linh said "loosen the parameters to get trades... at least 2 trades per month per asset." First fixed a real bug found in passing: `gate_htf_bias` re-derived HTF alignment independently from raw weekly/daily trend, using a rule stricter than (and inconsistent with) `bias.compute_bias`'s own S:4 logic — a setup with a legitimately-computed bias could still fail its own gate. Fixed to just trust `bias.direction`. Then funnel-traced the real bottleneck against actual history rather than guessing at knobs: removing the session/kill-zone filter, loosening `swing_n_weekly` 3→2, and — the single biggest lever — `disp_atr_mult` 1.5→0.75 (only ~12% of real MSS events had a strong enough breaking candle to leave a valid FVG/OB at the spec default), then `min_rr` 2.0→0.8 once that stopped being the bottleneck. Exposed all of these as `run_backtest_single_asset` parameters (previously hardcoded); added `tools/run_ict_backtest.py --calibrated` as the documented reference config.
- **Result**: even after roughly halving two core spec thresholds, actual filled trades reached only 0.45-0.74/month/asset (target was 2/month) — the ~30% qualified-to-filled conversion rate is now the limiting factor, not investigated further this session. **More importantly: 3 of 4 assets are net losing at this calibration** (BTC PF 0.73, ETH PF 0.54, **SOL PF 0.06 / 9.1% win rate / 77.7% max drawdown** — a real quality red flag). Only TAO is solid (PF 1.31, +$522, 61.5% win rate). Presented this tradeoff explicitly to Linh via AskUserQuestion (accept-and-push vs. dial back toward quality vs. investigate fill conversion vs. drop SOL) — **Linh chose accept-and-push, eyes open on the risk**. Pushed to VPS staging + GitHub (`c7a8bc4..0d067d3`).
- **Same session, continued a third time — fill-conversion fix**: see the Active State entry above (commit `94bb5c8`) — found and fixed a real graze-vs-genuine-retrace bug in `_invalidated`, +21% trades, SOL de-risked but still net losing.
- **Same session, continued a fourth time — validation pass**: see the Active State entries above (commit `053906c`) — split-sample check shows no asset is consistent across the two halves of the window; SOL's badness isn't SOL-specific (all 4 assets had 60-81% peak-to-trough declines this window) but more likely small-sample variance.
- **Handoff to**: Linh / next agent — **do not move to Phase 3 (scanner) or any paper/live worker on this strategy yet.** The engine is mechanically correct and thoroughly tested (129 unit tests, real-data-validated pipeline, several real bugs found and fixed this session), but the STRATEGY itself has not demonstrated a consistent edge — every asset's performance flips sign or degrades between the first and second half of the 2-year test window, and 11-21 trades/asset is too small a sample to resolve "no edge" from "not enough data yet." Two honest paths forward, Linh's call: (a) fetch a longer history (>2yr, if available) and/or wait to accumulate more forward data before drawing conclusions — small-sample noise may simply need more samples; (b) accept the uncertainty and treat a future paper-trading phase itself as the real out-of-sample test, with correspondingly cautious sizing. Either way, do not skip straight to Phase 3/live on the strength of this backtest alone. Next agent: read this entry + `tools/split_sample_check.py`'s output pattern before making that call.

### Session 15 (2026-07-13) — `min_tp_pct` FIX DEPLOYED (measurement pending) + SCALP STRATEGY PLANNED
- **`min_tp_pct` resolution**: after a 4th zero-trade check (overnight 07-14→15: 0 trades all assets, PID 2248353 still alive), Linh chose the **guard reorder**. Implemented in `execution.py::_structural_sl_tp()` (R:R extension before the `min_tp_pct` floor), commit `c78dcf8`, verified by head-to-head unit test. Deployed via **content-based patch** (`tools/patch_guard_reorder.py`, self-tested, idempotent) — deliberately NOT a whole-file scp, to keep the still-undeployed session-12 position-sizing fix out of this deploy (one variable). Deploy doc + rollback: `deploy-guard-reorder-2026-07-13.md`. **DEPLOYED 2026-07-14 22:56, new PID 2433063; reorder confirmed live via grep** (Guard 2 = Soft R:R, Guard 3 = Option B floor). Measurement pending — baseline `bot.log` L0=29148 for scoped reject counts; re-check trade frequency over 3–5 days. Live-money change. Next gate to watch: `min_profit_usd: 5.0`. Once any asset hits 5 closed trades the reflection loop activates for the first time. **MEASUREMENT RISK**: BTC/ETH/SOL `trades.jsonl` still 0 (logging bug open since s11) — must confirm trades actually WRITE there when they fire, else we're blind to the fix's effect (TAO/XRP file-writing works, so the path can).
- **Trigger**: Linh asked "why no trades" again, then asked to plan a new multi-TF S/R **scalping** strategy (VWAP + 50MA + 200MA) and assess its impact on the current strategies.
- **Code changed locally, nothing deployed** (sandbox still has no VPS access). The diagnosis was re-derived from source; then Linh's decision led to the one code change this session — the guard reorder, commit `c78dcf8`, execution.py only. VPS unchanged (still running the 07-03 process); deploy is queued for Linh.
- **Diagnosis (part A)**: `min_tp_pct: 3.0%` still binding. Two additions over session 14 — (1) mechanistic explanation of *why* it over-rejects on a 15m structural-TP strategy (TP = nearest swing, then rejected for being near), and (2) a **guard-ordering finding** (Guard 2 hard-reject before Guard 3 R:R-extension) that yields a cleaner fix than lowering the number. Three options for Linh: reorder guards (recommended), lower `min_tp_pct` to 1.5–2.0%, or keep 3% and widen structural lookback. Doc: `diagnosis-session-15-2026-07-13.md`.
- **Scalp plan (part B)**: decisions locked with Linh — 15m, separate isolated worker, several assets. Design: second `setsid` process with `STATE_DIR=state-scalp`, own goal + per-asset configs, fully inverted gate set; shared `price.py` gets net-new `sma_200` + `mtf_sr_zone` (additive, regression-tested) and a fetch-limit bump. Impact on live strategies: none if isolated as planned; the one managed coupling is the `price.py` limit change. Docs: `plan-scalp-strategy-2026-07-13.md` (full plan + impact analysis), `state-scalp/goal.yaml` + `state-scalp/_scalp-strategy-template.yaml` (drafts), `deploy-scalp-step1-2026-07-13.md` (indicators + regression test, local-only, no VPS).
- **Handoff to**: Linh. Fix is DEPLOYED (2026-07-14 22:56, PID 2433063, confirmed live). Immediate: over the next few hours/days, re-check trade frequency scoped from `bot.log` L0=29148 (`tail -n +29148 ... | grep -c "Structural TP too thin"` should fall; trades.jsonl counts should rise), and confirm BTC/ETH/SOL trades.jsonl actually writes when trades fire. Then check `min_profit_usd` as the next gate. Still open: four scalp decisions before that build starts — SMA-200 vs EMA-200, `price.py` approach (limit bump vs higher-TF derive), final scalp asset basket, scalp sizing floor (lower `min_profit_usd` vs size up). Next agent: read the three 07-13 docs; build the scalp only after Linh's calls; one variable per deploy.

### Session 14 (2026-07-08) — ROOT CAUSE FOUND, AWAITING LINH'S DECISION ON `min_tp_pct`
- **Trigger**: Linh asked "still no trades" again — session 13's diagnostic (written 07-06) was never run; this is the same open question, now 5 days post-deploy instead of 3.
- **No code or config changed.** VPS unreachable from this sandbox as always; Linh ran three rounds of SSH diagnostics this session.
- **Path to the answer**: started from local code review (confirmed soft-discount logic correct; found the untouched ambiguous-band hard gate; raised a reflection-drift hypothesis) — see the two intermediate diagnostic rounds in `diagnosis-session-14-2026-07-08.md` for the false starts (a `FIRE`-string grep that returned a misleading 0, and an unscoped full-log count contaminated by pre-deploy history from `bot.log`'s append mode). Final round scoped counts to the currently-running process only (`PID 2248353`, running continuously since the exact 07-03 11:35 deploy restart — confirmed via `ps -o lstart,etime`).
- **Root cause confirmed**: trend filter fix works (4h-disagree: 6, ambiguous-band: 59 — both negligible over 4d20h). Reflection drift ruled out (`hypotheses.jsonl` empty for all 5 assets on the live VPS — no asset has reached the 5-closed-trade threshold to trigger even one reflection). The actual dominant gate is **`min_tp_pct: 3.0%`** in `execution.py` — 3255 rejections across BTC(982)/ETH(1057)/SOL(673)/TAO(332)/XRP(211), more than the session-time-block (2240). This gate only evaluates after a signal clears every upstream gate, so it means plenty of signals ARE firing — they're dying at the last step before trade placement. Session 12 had flagged this as "SOL's separate problem"; the data shows it's actually cross-asset and dominant, correcting that assumption (see Key Decisions table).
- **Next agent / Linh**: decide whether to lower `min_tp_pct` (frequency should recover sharply) or keep it as a deliberate 3% quality floor. Nothing implemented yet — flagged per standing instruction, no deploy without Linh's sign-off.

### Session 13 (2026-07-06) — DIAGNOSTIC WRITTEN, AWAITING LINH'S OUTPUT
- **Trigger**: Linh asked "still no trades, what's going on?" — 3 days after the session 12 trend-filter soft-gate deploy, exactly at that deploy doc's own "re-check after 3-5 days" checkpoint.
- **No code or config changed.** This sandbox has no VPS network access (re-confirmed: `/dev/tcp/187.127.108.173/22` unreachable), so diagnosis requires Linh to run an SSH block and paste back output, same pattern as session 12.
- **Wrote** `diagnosis-session-13-2026-07-06.md`: checks bot/heartbeats are alive, trade counts since 07-03, whether `trend_4h_soft_discount` is actually present in the running `loop.py` (rules out a deploy that silently didn't take), and a skip-reason tally to identify whichever gate (confidence, indicator count, trend filter, SOL's `min_tp_pct`, session filter, portfolio loss cap) is currently binding.
- **Also noted**: the leverage/position-sizing fix from session 12 (`d8a49c7`, `75ef76b`) is implemented locally but still NOT deployed — deliberately held back to isolate the trend-filter fix's effect on frequency. Unrelated to "no trades," don't bundle it into whatever fix comes out of this diagnostic.
- **Handoff to**: Linh, to run the diagnostic block and share the output. Next agent should read `diagnosis-session-13-2026-07-06.md` + `hermes-trading-improvement-plan-2026-07-03.md` Part 2/4 before deciding which gate to loosen, if any.

### Session 12 (2026-07-03) — DIAGNOSED + FIXED LOCALLY, VPS DEPLOY PENDING
- **Trigger**: Linh reported ~1 trade in the 15 days since session 11.
- **Diagnosed**: this sandbox has no VPS network access, so Linh ran a read-only SSH diagnostic block (process/heartbeat/trade-counts/skip-reason-grep/live-vs-git-drift). Result: bot healthy (all heartbeats fresh, no crash), but BTC and XRP were hard-skipping on the daily/4h trend filter's AND-gate on literally every tick for a full 3-hour sample — daily EMA(20) and 4h EMA(50) were on opposite sides of price the entire window, a sustained regime, not a brief crossover.
- **Fixed**: `_get_trend_direction()` in `loop.py` now returns `(direction, confirmed)` instead of `str | None`. Daily EMA bias is still a hard gate (unchanged — stopped the June losses). 4h disagreement now multiplies confidence by `trend_4h_soft_discount` (0.7, new yaml key on all 4 active assets) instead of hard-skipping. Linh chose this over "drop 4h entirely" or "OR logic" alternatives.
- **Validated**: `py_compile` clean; manually re-ran the new function against the exact BTC/XRP EMA values from the live diagnostic and confirmed it now returns `('short', False)` (soft-discount path) instead of the old hard `None` (skip).
- **Also found, deliberately NOT touched this session**: BTC/ETH/SOL `trades.jsonl` still empty on VPS (session 11's own "fix dead cron" item, never done); SOL blocked by an unrelated `min_tp_pct` structural guard; ETH absent from the log sample; hyphen git clone on VPS is stale (not dangerous — running code has the correct config, just needs a `git pull` before it's used as a diff baseline again).
- **Deliverables**: `hermes-trading-improvement-plan-2026-07-03.md` (full diagnosis + Part 3 live results), `deploy-trend-filter-soft-gate-2026-07-03.md` (8-step deploy block).
- **Handoff to**: Linh, to run the deploy doc, then re-check trade frequency after 3–5 days before considering any further gate change.

### Session 11 (2026-06-18) — CODE COMPLETE, VPS DEPLOY PENDING
- **Status**: All code changes implemented locally. Pending VPS deploy by Linh.
- **Deploy guide**: `deploy-overhaul-2026-06-18.md` — 9-step guide with exact scp/ssh commands. Run in order.
- **What changed locally** (all validated with py_compile):
  - `hermes_trading/adapters/price.py` — added `ema20_daily` + `ema50_4h` to price dict
  - `hermes_trading/loop.py` — session filter, portfolio daily loss cap, trend filter gate, `trading_enabled` flag
  - `hermes_trading/adapters/execution.py` — new `_position_based_sizing()` (10% bal, 2% risk, 3–8x leverage)
  - `dashboard.py` — TAO→XRP, trend filter badge, PAPER/PAUSED badge, long/short win rate, portfolio daily P&L
  - `state/btc_usdt/strategy.yaml` → v03 (trend_filter, position_pct, risk_per_trade, session filter, loss cap)
  - `state/eth_usdt/strategy.yaml` → v04 (same as BTC)
  - `state/sol_usdt/strategy.yaml` → v02 (same as BTC)
  - `state/tao_usdt/strategy.yaml` → v05 (`trading_enabled: false`)
  - `state/xrp_usdt/strategy.yaml` → NEW v01 (paper mode, all session 11 config)
- **Linh's deploy steps** (from deploy-overhaul-2026-06-18.md):
  1. Stop hermes workers
  2. Backup VPS code
  3. scp 4 Python files to VPS
  4. scp 5 strategy YAMLs to VPS
  5. Validate syntax on VPS (py_compile)
  6. Fix dead cron + run 7-day backfill to restore trades.jsonl
  7. Create hermes-xrp systemd unit + restart BTC/ETH/SOL/XRP (TAO stays stopped)
  8. Verify heartbeats
  9. Smoke-check new filter log lines
- **After first 10 XRP paper trades**: review win rate → if positive, set `trading_mode: live` in `state/xrp_usdt/strategy.yaml` and redeploy that file only.
- **Next session focus**: review first week of post-overhaul trade data; assess if trend filter is filtering correctly or being too restrictive (target: <40 trades/week, >55% win rate).


- **Status (end of session 6, 2026-05-28)**: Phase 2.4 DEPLOYED to VPS. Commit `f0f0893` pushed; running agent restarted (replaced PID 1320187 with fresh process launched from `/opt/trading/hermes_trading`). New `min_tp_pct` guard confirmed firing in live log (BTC skip at 01:29 UTC: "Structural TP too thin: 0.02% < min 3.00%"). No IndentationError on agent restart → reflect.py truncate fix took effect. Backfill ran with `bybit=0` (likely Bybit V5 7-day query window quirk; TAO already has 23 native-logged trades → reflection unblocked anyway).
- **Deploy mishap logged for doctrine**: 5-step deploy block in `session-6-phase-2.4.md` had Step 2 combining `diff` + `cp` in two code blocks under one heading; Linh ran only the diff and skipped the cp, then hit `ModuleNotFoundError` on backfill. Recovery: one extra ssh cp command. Lesson: every deploy step should be ONE code block, ONE command. Update template before next deploy.
- **Env var pickup**: backfill needed `set -a; source .env; set +a` prefix because `python -m tools.backfill_trades` doesn't auto-load `.env` (execution.py uses `os.getenv` directly, not python-dotenv). Same prefix is needed for the agent restart command. Add to the runbook.
- **Anomaly flagged for separate session**: Bybit account shows `cumRealisedPnl: -18272.77 USDT` per the balance debug print. Could be historical test losses or real accumulated loss. Worth an audit session: pull full closed-PnL history, bucket by date, identify the bleed.
- **Next-session candidate**: Phase 2.5 (`fetch_last_closed_pnl` direction-aware pnl_pct) + Phase 2.3 (`fetch_recent_closed_trades` for dashboard merge) — same function, do together.
- **Status (end of session 5c)**: VPS agent is RUNNING — PID 1290662, all 4 workers booted in live mode. Awaiting first 15m tick to confirm `dir=both · conf=NN%` lines and absence of new 110043 tracebacks.
- **Linh to do next**:
  1. `tail -f /opt/trading/hermes_trading/bot.log` and verify first tick shows real conf% per asset
  2. If first 2-3 ticks show `dir=both · conf=0%` across the board, signal is genuinely flat (different problem — would need to inspect price adapter output)
  3. If `Trade #1` fires: verify on dashboard that R:R column shows the structural value, and that SL/TP are at swing levels not fixed % distances
  4. SCP `/tmp/hermes-backup-<STAMP>/` to local if you want to preserve pre-session-5 yamls (else they'll vanish on VPS reboot)
  5. Consider Option B (collapse to single folder) vs Option A (keep two-folder pattern) for the `/opt/trading/hermes-trading/` + `/opt/trading/hermes_trading/` situation — separate session
- **Carried over from session 4**: dashboard Live Positions R:R display (verify on next live tick)
- **Handoff to**: Linh (monitor + decide on next-session topics)

#### Session 5d — Strategy review document
- Linh raised 6 strategy questions (min R:R 2.0, TP zones, trailing stops, dashboard-Bybit reconciliation, swing vs SMC style, 3% return target).
- Wrote up answers in `strategy-review-session-5.md` with: empirical context from TAO Trade #1 (R:R 1.73, conf 54.76%), per-question analysis, proposed Phase 2 implementation order (min TP guard + min R:R + target-return filter in one session; trailing stops next; Bybit backfill after; TP zones deferred to Phase 3).
- **No code or YAML changes.** Phase 2 starts only after Linh answers 5 open questions in the doc (soft vs hard R:R guard, return-enforcement option A/B/C, trailing stop fixed vs ATR, timeframe 15m vs 1h, duplicate-trade-record bug timing).
- **Anomaly logged for later:** TAO trades.jsonl had 1 line at scp time, 4 identical lines at later ssh-cat time. Only one real Bybit order was placed. Pure bookkeeping bug. Defer to post-Phase-2.

#### Session 5e — Phase 2.1 implementation (2026-05-27)
- **Decisions locked**: soft R:R (extend TP, don't skip), Option B target-return filter, ATR-based trail next, dupe-bug deferred.
- **execution.py rewritten** with three guards in `_structural_sl_tp`: max_sl_pct (existing), min_tp_pct (new), min_rr_ratio (new soft). Written atomically via Python (Write tool truncated on Windows mount AGAIN — same hazard as Edit, now confirmed both tools have this failure mode).
- **All 4 strategy.yamls** got `min_tp_pct: 3.0` and `min_rr_ratio: 2.0`.
- **6 unit tests pass.** Crucially: TAO Trade #1 replay (entry $282, support $278.8 / 1.13% from entry) **correctly skips** under new Option B filter. That trade would NOT fire under new rules.
- **Commit `55cbbaa`** landed locally despite `.git/index.lock` permissions noise on Windows mount.
- **Push to GitHub**: pending Linh's PowerShell.
- **Deploy to VPS**: pending Linh's PowerShell + SSH.
- **Expected post-deploy effect**: trade frequency drops 60-80%; trades that fire have R:R ≥ 2.0 and structural target ≥ 3% price move; the 10001 "TP too close" Bybit rejection on BTC/ETH should disappear since 3% >> typical slippage.

### Persistent operational issue: Windows-mount file tool corruption
- Both `Edit` AND `Write` tools have truncated files on the C:\Users\nghil mount. Always use `python -c '...'` or `python <<EOF` heredoc via bash for Python file changes. Validate with `python -m py_compile` immediately. Never trust the file tool's "success" response for Python files on this mount.

### SSH setup for dashboard (session 5f, 2026-05-27)
- Local Windows machine had NO ssh keys at all (only known_hosts). Dashboard's `BatchMode=yes` SSH was falling through to password auth, which the VPS rejects for root (standard `PermitRootLogin prohibit-password` policy).
- Generated `C:\Users\nghil\.ssh\hermes_vps` (passphraseless ed25519) on Windows.
- Added the pubkey to VPS `/root/.ssh/authorized_keys` (preserved the pre-existing `hermes-dashboard` orphan key).
- Added `Host 187.127.108.173` block to `~/.ssh/config` with `IdentityFile ~/.ssh/hermes_vps` and `IdentitiesOnly yes`.
- `ssh -o BatchMode=yes ... "echo OK"` now succeeds → dashboard's `_ssh_batch` works.
- **Tripwire to remember**: the pre-existing `hermes-dashboard` key entry in authorized_keys had no trailing newline. `echo >> file` concatenated the appended line onto the same line, corrupting both entries. Always rebuild authorized_keys via heredoc (`cat > file <<'EOF' ... EOF`) and verify with `wc -l` afterward.

## Session Log

### 2026-07-03 (session 12) — Trade frequency diagnosis + trend filter soft-gate fix

Linh reported ~1 trade across all 4 assets in the 15 days since session 11. Reviewed git log, all 4 strategy yamls, and `loop.py`'s entry-gate logic (`_evaluate_entry`, `_get_trend_direction`) and wrote up an initial gate-stack hypothesis plus a read-only VPS diagnostic block (this sandbox has no VPS network access).

**Linh ran the diagnostic.** Results: bot healthy, all heartbeats fresh, no crash. But BTC and XRP hard-skipped on the daily/4h trend filter's AND-gate on every single tick across a full 3-hour log sample — daily EMA(20) and 4h EMA(50) sat on opposite sides of price the entire window (BTC: price below daily EMA → bearish bias, but above 4h EMA → bullish; same pattern on XRP). That's a sustained regime, not a brief crossover, and it's why frequency collapsed so much further than session 11's own 5–10/week estimate. SOL turned out to have a separate, unrelated problem (`min_tp_pct` structural guard). BTC/ETH/SOL `trades.jsonl` are still empty on VPS — session 11's "fix dead cron" item was never done.

**Fix implemented and validated locally**: `_get_trend_direction()` now returns `(direction, confirmed)`. Daily EMA bias stays a hard gate (unchanged). 4h disagreement now discounts confidence by `trend_4h_soft_discount` (0.7, new key in all 4 active yamls) instead of hard-skipping — Linh's choice over dropping 4h entirely or switching to OR logic. Validated with `py_compile` and by re-running the new function against the exact live BTC/XRP values, confirming the soft-discount path fires where the old code hard-skipped.

**Not deployed yet** — `deploy-trend-filter-soft-gate-2026-07-03.md` has the 8-step block. Empty trades.jsonl / dead cron, SOL's TP guard, and the stale hyphen git clone are logged as separate follow-ups, deliberately not bundled into this deploy.

**Trend-filter deploy — completed and confirmed live.** Hyphen clone pulled clean (was stale at `36de5ee`, now at `f4746bc`). Backup step needed a fix — copying 4 `strategy.yaml` files with identical basenames into one directory in a single `cp` call clobbers itself; switched to 4 explicit named copies. Deploy hit two more issues worth recording:

1. **PowerShell mangles `$varname` inside double-quoted `"..."` strings even with a backslash in front** — backslash isn't PowerShell's escape character (backtick is). Every ssh command containing a bash `$variable` (loop vars, `$(wc -l ...)`, etc.) needs a PowerShell single-quoted here-string (`@'...'@ | ssh host bash`) instead of a quoted command string. Hit this three times this session before consistently switching to here-strings.
2. **The restart command silently failed to launch a new process.** `pkill -f hermes_trading.run; ...; nohup .venv/bin/python -m hermes_trading.run >> bot.log 2>&1 & echo launched && echo DONE` printed nothing at all and left the bot down for ~1 hour with zero ticks (confirmed via `ps aux` showing no matching process, heartbeats frozen, and no new lines in bot.log — not even a crash traceback, it just never started). Best explanation: backgrounding an entire `cd && source .env && ... && nohup ...` chain with a trailing bare `&` inside a non-interactive SSH command is fragile — the SSH session can close before the chain finishes detaching. **Fix**: `setsid <cmd> >> bot.log 2>&1 < /dev/null &` on its own line, every setup step (`cd`, `source .env`) on its own separate line rather than chained with `&&` into the backgrounded command, and stdin explicitly redirected from `/dev/null`. Confirmed working — PID materialized immediately, verified via `ps -eo pid,cmd`. **Doctrine update**: prefer this exact pattern for every future VPS restart over the old `nohup ... & disown`/`setsid ...` one-liners documented in earlier sessions (doctrine #17/#18 partially superseded — those still had the "everything chained with && then backgrounded as one unit" problem).

**Post-restart confirmation (11:35 UTC tick)**: BTC/ETH/SOL/XRP all show `No entry · dir=... · conf=...%` instead of the old blanket `Trend filter: ambiguous or daily/4h mismatch`. BTC now evaluates `dir=short` at 33% confidence — previously would have hard-skipped without computing a confidence number at all. Fix is confirmed live and working as designed.

**Same session, second request**: Linh asked for a hard minimum trade size (10% of available margin), then to make it a floor that scales up with confidence rather than staying fixed. Investigated `_position_based_sizing()` — it already computed exactly `balance × position_pct` (10%, and `balance` is already Bybit's `free`/available margin via `_fetch_usdt_balance`, not total equity) — so the floor already existed as a fixed value, just not literally a "minimum." Added `_confidence_to_position_pct()` (piecewise linear over new `position_pct_curve`, same pattern as the existing `leverage_curve`), clamped to never go below the base `position_pct`. All 4 active yamls get `position_pct_curve: [[0.5,0.10],[0.75,0.15],[1.0,0.20]]`. Separate deploy doc (`deploy-position-sizing-floor-2026-07-03.md`) — hold until the trend-filter deploy is confirmed, per the one-change-at-a-time discipline.

**Resolved**: pulled the actual 2026-06-25 XRP trade record. qty=68.3, entry=1.0373, leverage=4 → notional $70.85, implying ~$708 balance at the time (not far off the $800 illustrative figure — that part was fine). The real discrepancy: notional should have been margin×leverage = $283.40 per the sizing docstring's own logic, but the code only ever produced margin-equivalent notional ($70.85, unleveraged) — leverage was set on Bybit but never multiplied into qty. Linh confirmed the intended model directly ("if balance is 708, minimum size is 70.80 then leverage applied to that, if 2x then total is 140") — this is now implemented and verified against session 11's own worked examples (see Key Decisions). This was a real bug, live since session 11, not a documentation-only issue.

### 2026-06-18 (session 11) — Deep strategy review + overhaul recommendations

Linh flagged two weeks of dismal results. Full analysis of trade data, session history, current strategy config, and all known issues produced a comprehensive overhaul document.

**Data analysed**: Bybit CSV (155 trades, May 21–Jun 18 — full period), all 4 strategy.yamls (post-session 15 state), full session history.

**Key findings (full 155-trade dataset)**:
1. **May**: 51 trades, 57% win rate, +$131.37. **June**: 104 trades, 37% win rate, -$105.05. Account is +$26 cumulative — almost wiped out a strong May.
2. **Long entries are broken across all assets**: TAO 34%, ETH 33%, SOL 37%, BTC 29% win rates on longs. Short entries are much better: TAO 46%, ETH 55%, SOL 54%, BTC 86%.
3. The bot is systematically buying RSI-oversold lows into ongoing downtrends (no 1h trend filter). June 7 worst example: 5 consecutive TAO shorts into a +11% uptrend = -$85 in one day.
4. June 2: BTC dumped 4.6%, bot entered longs on all 4 assets simultaneously, all hit SL. Portfolio correlation + no daily loss cap = -$51 in an afternoon.
5. All 4 trades.jsonl are EMPTY on VPS — cron backfill dead, Hermes reflection offline for unknown duration.
6. `risk_per_trade: 10%` means each SL hit = ~$10. Magnifies the direction problem 5-10× vs 2% risk.
7. BTC is the only consistently profitable asset: 57% overall, 86% short win rate. The only one worth keeping at current settings.

**Confirmed decisions (Linh answers locked in):**
- TAO out → replaced with XRP/USDT (paper mode first, 10+ trades before live)
- Daily EMA(20) + 4h EMA(50) trend filter: long only above both, short only below both
- Position sizing: 10% of balance notional, leverage scales dynamically (risk_pct / position_pct / sl_dist), capped [3, 8x], targeting 2% max risk per trade (~$16 at $800 balance)
- BTC: unchanged, already performing (57% win rate, 86% short win rate)
- Cron diagnostic: Linh to SSH and check

**No code deployed this session.** Full overhaul doc at `strategy-overhaul-2026-06-18.md`. Next session: (1) cron fix + XRP paper setup, (2) daily/4h trend filter code, (3) new position sizing code, (4) portfolio daily loss cap.



### 2026-06-06 (session 9) — Confidence-scaled leverage

Replaced fixed 5x leverage with a confidence-scaled range of 3x–10x. Formula: `leverage = round(3 + (10 - 3) * confidence)`, clipped to [3, 10]. At 40% conf → 5.8x ≈ 6x; at 70% conf → 7.9x ≈ 8x; at 100% conf → 10x.

**Files changed** (commit `aafe0a1` + `a9e9b27`):
- `hermes_trading/adapters/execution.py`: leverage block replaced; reads `min_leverage`/`max_leverage` from strategy + `confidence` from `entry_detail`
- `hermes_trading/loop.py`: `_entry_gates_snapshot` updated to record `min_leverage`/`max_leverage` instead of `default_leverage`
- `hermes_trading/reflect.py`: tunable variable list now has `min_leverage` + `max_leverage` instead of `default_leverage`
- All 4 `state/*/strategy.yaml`: `default_leverage: 5` removed; `min_leverage: 3` + `max_leverage: 10` added
- `tools/fix_leverage_yaml.py` (NEW): one-shot script to patch VPS yamls (run once, kept for reference)

**Deploy notes**:
- Sandbox committed `aafe0a1` first (7 files); PowerShell commit `a9e9b27` added only `fix_leverage_yaml.py`
- VPS yamls patched via `fix_leverage_yaml.py` (removed old `max_leverage: 5` / `default_leverage: 5`, added `min_leverage: 3`), then `max_leverage: 10` appended via `echo` (script missed it due to VPS yaml shape diff)
- Bot restarted as PID 1653858, all 4 workers live, ticking at 03:14 UTC

**Doctrine note**: sandbox `git add` + `git commit` works but PowerShell git doesn't see it as pending (the index is shared). Commit from sandbox is valid and pushes cleanly — just looks surprising in `git status` from PowerShell afterward.

### 2026-06-08 (session 10) — Loss investigation + risk fixes

Linh flagged "too many losing trades." Investigation of CSV data (45 trades, May 23–29) and session history identified four root causes:

1. **TAO overtrading + fee bleed**: 16 TAO trades on May 27, -$9.78 PnL that day, $8.79 burned in fees. 15 of 24 wins across all assets were <$5 (several <$1, net losers after ~$0.55/trade fee drag).
2. **Flawed Hermes mutations on TAO**: v03 `volume_spike.min_ratio 1.5→2.0` based on hallucinated LLM claim ("80% of losers fired volume_spike" — decision_context showed wins had higher volume). v04 `bb_squeeze.weight 1.0→1.25` with garbled delta-sign reasoning.
3. **Confidence-scaled leverage (June 6)**: At typical 40-60% confidence entries, leverage rose from 5x to 6-7x with no corresponding edge — identified as driver of the most recent loss spike.
4. **Reflections dead May 28–June 5**: subprocess timeout 120s killed Hermes before Ollama responded. Fixed session 8.

**Fixes applied this session (local yamls + VPS deploy doc):**
- All 4 assets: `min_confidence: 0.3/0.4 → 0.5`
- All 4 assets: leverage pinned at 5x (`min_leverage: 5`, `max_leverage: 5`)
- TAO: `volume_spike.min_ratio 2.0 → 1.5` (VPS-side revert)
- TAO: `bb_squeeze.weight 1.25 → 0.3` (VPS-side revert)
- BTC: `max_trades_per_day: 10` added (was missing)

**Deploy doc**: `deploy-fixes-2026-06-08.md` — 9 steps, one SSH block each. Pending Linh push + deploy.
**Investigation doc**: `investigation-losing-trades-2026-06-08.md`.

**Leverage implementation**: New `_confidence_to_leverage()` in `execution.py` reads `leverage_curve` list from strategy yaml and does piecewise linear interpolation. The `min_leverage`/`max_leverage` fields are now fallback-only (used if no curve present). Hermes reflections can still tune those fallback values; the curve itself is the primary control.
**max_trades_per_day**: set to 3 for all assets. Enforcement is in loop.py's existing trade-count check (already present).
**Deploy doc**: `deploy-fixes-2026-06-08.md` — Step 5 is now split into 5a (execution.py cp) and 5b (yaml patch). Resume from Step 3 (backup).

### 2026-06-06 (session 8 continued) — Phase 2.3/2.8/2.9/2.10 + ETH signal quality

**Reflections confirmed working** after timeout fix: ETH (1 hypothesis, fallback), SOL (1), TAO (5, incl. new Hermes v03→v04). BTC still at 0 — likely insufficient closed trades.

**TAO v04 Hermes mutation** (20:45 UTC Jun 5): `indicators[bb_squeeze].weight 1.0 → 1.25`. LLM reasoning was partially garbled (misread delta sign — said "negative delta" when delta was +0.154). Change direction was coincidentally correct (bb_squeeze fires on wins, not losses). `old_value_mismatch` guard now catches this going forward.

**ETH analysis**: entering counter-trend longs at 33% confidence (RSI + VWAP only) with `support_1h4h: null` — using fallback SL. ETH fallback reflection tightened `stop_loss_pct 2.0 → 1.8` (triggered by 6.74% drawdown vs 5% goal). ETH NOT on circuit breaker (only 1 SL hit from Jun 3, outside 4h window).

**Changes shipped this sub-session** (commit `1e6a981`):
- `state/eth_usdt/strategy.yaml`: `min_confidence: 0.3 → 0.4` (blocks RSI+VWAP-only entries)
- `hermes_trading/reflect.py`: old_value sanity check before `_set_nested`; `trail_atr_mult` added to tunable variable list
- `hermes_trading/adapters/execution.py`: `fetch_recent_closed_trades(asset, limit=50)` (Phase 2.3)
- `tools/audit_historical_pnl.py` (NEW): paginated Bybit PnL audit tool
- All 4 `strategy.yaml`: `trail_atr_mult: 2.0` + version fields

**Bot restarted** at 22:02 UTC Jun 6 (PID 1640612). All 4 workers live. Guards firing correctly.

**Doctrine note**: `nohup ... &` (without `disown`) works for VPS restart via `ssh host "command"` on this VPS. `setsid` did NOT work. Use `nohup` going forward.

### 2026-06-05 (session 8) — Reflection subprocess timeout fix + bot restart
**Symptom**: All reflections silently failing since session 7 restart. TAO had 4 hypotheses (last from 2026-05-29), BTC/ETH/SOL had 0 despite 16/20/22 trades respectively. Log showed `Reflection failed: ... timed out after 119.99... seconds` on every trigger.

**Root cause**: `loop.py` line 627 had `subprocess.run(..., timeout=120)`. The `HERMES_LLM_TIMEOUT=300` fix from session 6 only raised the HTTP timeout inside `reflect.py` — the outer subprocess was still killed at 120s. Ollama IS healthy and responding; inference just takes >120s with 8K context.

**Fix**: `sed -i 's/timeout=120,/timeout=360,/'` on VPS, same edit applied locally, commit pending push.

**Secondary incident**: After patching, bot was restarted with `disown` via non-interactive SSH — `disown` doesn't work in non-interactive shells. Process appeared to start then vanished. Bot was down ~8 hours (23:30 Jun 4 → 23:40 Jun 5 UTC). All 4 positions were open during downtime, managed by exchange-side SL/TP only. Fixed by using `setsid` instead of `disown` for background daemonization via SSH.

**State at end of session**:
- Bot running as PID 1606955, all 4 workers live
- All 4 assets have open positions (all ticking "position already open, skipping")
- TAO at 40 trades — next reflection fires at 45; should now complete with 360s timeout
- Commit `loop.py timeout=360` pending Linh push from PowerShell

**Doctrine added**:
17. **`disown` does not work in non-interactive SSH one-liners.** Use `setsid .venv/bin/python ... &` instead for daemonizing via `ssh host "command"`. `nohup ... & disown` only works in interactive sessions.
18. **The LLM call timeout and the subprocess timeout are independent.** `HERMES_LLM_TIMEOUT` in `.env` controls the HTTP request inside `reflect.py`. `subprocess.run(..., timeout=N)` in `loop.py` is a separate ceiling. Both must be set consistently: subprocess timeout should be > LLM timeout + startup overhead (current: LLM=300, subprocess=360).

### 2026-06-02 (session 7) — Emergency restart after 3-day downtime + 7-day backfill + 4h cron
**Symptom**: Cowork open, asked for `next-session-prompt.md` review. Diagnostic SSH probe revealed bot process gone, all 4 heartbeats stale at `2026-05-29T06:30:04 UTC` — **3 days down**. No process, no auto-reflection had fired, VPS uptime 69 days (no reboot), no OOM, disk fine.

**Root cause (chronology reconstructed)**:
1. Session 6 wave 4 prepared the audit-fields + None-safety patch (commit `ee0dea2`) touching loop.py + execution.py + reflect.py.
2. Deploy block on 2026-05-29 was run partially — loop.py and execution.py made it to underscore clone, **reflect.py did not** initially.
3. Bot restarted with NEW loop/execution + OLD reflect.py.
4. Bot ran ~6h. TAO Trade #25 triggered `--hermes` reflection at 04:00 UTC → crashed at reflect.py:434 with `TypeError: float(None)` (the exact bug wave-4 None-safety was meant to fix).
5. Reflection subprocess error swallowed by loop.py; bot kept ticking until 06:30 UTC.
6. At some point between crash and 06:30: Linh manually `cp`'d the fixed reflect.py (hyphen → underscore) and `pkill`'d to restart — but never re-launched. **Bot dark for 3 days.**

**Restart this session (8 steps, one block per action per doctrine #5)**:
1. `git push origin master` from PowerShell → "Everything up-to-date" (all fixes already on GitHub).
2. `git pull` on hyphen clone → only memory.md updated (reflect.py fix was already pulled previously).
3. `diff` hyphen vs underscore reflect.py → empty (already in sync — confirming Linh did the catch-up cp).
4-5. Skipped — files identical.
6. Verification block: None-safety filter ×5 ✓, audit fields ×12 ✓, wealth-curve marker ×6 ✓, line 434 = `options.num_ctx` (not crash line) ✓, py_compile OK ✓.
7. Restart: `pkill -f hermes_trading.run; set -a; source .env; set +a; nohup .venv/bin/python -m hermes_trading.run >> bot.log 2>&1 & disown`. PID 1525537, cwd `/opt/trading/hermes_trading`, all 4 workers booted.
8. Verify: 4 workers in log, first tick at 10:21 UTC processed reconciles cleanly.

**Reconcile during restart (positions managed exchange-side via SL/TP-on-order during downtime)**:
- ETH: closed by TP / exchange-side at +1.87% (reason `manual_or_other` — meaning closed without bot knowing tp/sl source)
- SOL: closed at +1.75% (same)
- TAO: closed by SL_hit at -0.65% (**first trade post v03 Hermes mutation `volume_spike.min_ratio 1.5→2.0`** — one-sample warning, see watchlist)
- BTC: no position (still blocked by `min_tp_pct: 3.0`)

**Doctrine added this session**:
14. **A partial deploy is invisible until something needs the missing piece.** Wave 4 cp landed 2 of 3 files; bot ran fine for 6 h until reflection demanded the third. **Defense**: after any multi-file deploy, run a `diff -r` between hyphen-clone and underscore-clone for the package dir as a final check. Treat any non-empty diff as deploy-incomplete.
15. **Heartbeat staleness is a higher-signal alarm than process-exists.** Process-down is obvious; heartbeat-stale-but-process-up would catch hangs too. Worth a passive cron on VPS: every 30 min, alert if any heartbeat is >20 min old.
16. **PowerShell here-string → ssh bash carries CR-LF — the LAST argument of the LAST command gets `\r` appended.** Tools that compare arguments to a known set (argparse `--dry-run`, file open, exec lookup) fail: `--dry-run\r` ≠ `--dry-run`; `tail bot.log\r` looks for a literal `bot.log\r` file. **Mandatory workaround for every PowerShell `@'...'@ | ssh bash` block: end the body with a trailing throwaway line like `echo DONE` so the `\r` corruption lands on something tolerant.** Confirmed twice in session 7: `tail bot.log` and `--dry-run` both failed without the throwaway, both worked with it.

**Open watchlist post-restart**:
- TAO v03 SL_hit on first post-mutation trade (1 sample, irrelevant alone — but Phase 2.8 sanity-check is worth landing before the next 5-trade boundary fires Hermes again).
- Intermediate trades during downtime gap (reconcile caught only the most recent close per asset) — `tools/backfill_trades.py` should pull the full 7-day window to recover any missing records.
- cumRealisedPnl on Bybit balance now `-18,251` (was `-18,272` at session-6 end) — tiny improvement from exchange-side TP fills during downtime.

**Files touched this session**:
- `session-7-restart.md` (NEW) — full diagnostic + deploy block
- `memory.md` — session 7 entry + doctrine #14, #15, #16 (and #16 fix verified twice live)
- `next-session-prompt.md` — updated current state + reordered backlog
- `README.md` — full rewrite for current state (live trading, SMC, Hermes, audit fields, dashboard, doctrine)
- `tools/cron_backfill.sh` (NEW) — wrapper for 4-hour cron-driven Bybit closed-trade sync
- `archive/` (NEW dir) — moved `diagnosis-session-5.md`, `strategy-review-session-5.md`, `session-6-phase-2.4.md`
- 7 May-22 setup docs deleted (DEPLOY-GUIDE, FINAL-VPS-COMMANDS, FIX-VPS-SHAPOT, PUSH-TO-GITHUB, VPS-COPY-INSTuctions, VPS-RUN-commands, VPS-SETUP-COMMANDS)

**Cron installed on VPS** (root's crontab):
- `0 14 * * * /opt/trading/hermes_trading/snapshot.sh ...` (pre-existing daily snapshot)
- `0 */4 * * * /opt/trading/hermes_trading/tools/cron_backfill.sh` (NEW: every 4h closed-trade backfill)

**Backfill outcome (one-time 7-day catch-up)**: +38 trades landed across all 4 assets (BTC +4, ETH +6, SOL +3, TAO +25). Dashboard now sees Bybit truth.

**Handoff**:
- **To**: Linh (monitor + decide whether to run backfill / start Phase 2.8 / wait for stability before Phase 2.2)
- **Read first next session**: `next-session-prompt.md` (update after this session), then this memory.md entry, then `session-7-restart.md` if context is needed
- **Phase 2.2 (ATR trailing stop) remains pending** — paused until bot is verified stable ≥24 h

### 2026-05-28 (session 6) — Phase 2.4 reflection rebuild + $5 hard guard
- **reflect.py was corrupted** with duplicated `run_hermes` + `main` bodies at lines 572-701 (prior Edit-tool corruption). Final 5 lines had `console.print` truncated to bare `print` at module-level indent → `IndentationError` on line 572. Reflection subprocess had been failing on every invocation since.
- **Fix:** truncated cleanly at the first occurrence of `if __name__ == "__main__":\n    main()\n`. File 702 → 572 lines. Validated with `python -m py_compile` and `ast.parse`. Written atomically via Python heredoc + `.tmp` → `replace()` (never trust Edit/Write on the Windows mount; lesson re-confirmed).
- **TAO trade visibility:** local `state/tao_usdt/trades.jsonl` had 1 line (the open Trade #1 from session 5). Linh reported "lots of small TAO short trades" — those were all on Bybit, invisible locally. `_count_closed_trades` was therefore returning ~0 for every asset and reflection was never crossing its cadence threshold.
- **New `tools/backfill_trades.py`** — one-shot Bybit closed-trade backfill, calls `private_get_v5_position_closed_pnl` per asset (paginated via `nextPageCursor`, 30-day default lookback), appends missing records to `trades.jsonl` deduped by `order_id`. Idempotent. **Direction-correct pnl_pct** computed as `closedPnl / cumEntryValue` (sidesteps the broken `(exit-entry)/entry` in `fetch_last_closed_pnl` — that's Phase 2.5). Synthetic records flagged `backfilled: true`, `strategy_version: "backfilled"`. CLI supports `--asset`, `--dry-run`, `--lookback-days`. New `tools/__init__.py` so `python -m tools.backfill_trades` works.
- **New $5/trade hard guard** (`_guard_min_profit_usd` in execution.py). Called in `place_live_trade` between `_risk_based_qty` and `create_order`. Raises ValueError if `qty × |tp − entry| < min_profit_usd`. Loop catches and logs "Entry skipped — Expected TP profit too small: $X.XX < min $5.00". Added `min_profit_usd: 5.0` to all 4 strategy.yamls.
- **Test coverage:** 6 backfill unit tests (winning-short → +pnl, losing-long → -pnl, malformed → None, dedup, idempotency, dry-run safety) + 6 guard tests (floor pass/fail, custom floor, zero-profit edge, TAO Trade #1 replay still blocked by min_tp_pct, constructed trade passing Phase 2.1 but failing $-guard). All green.
- **TAO Trade #1 (entry $282, R:R 1.73) would not fire under any combination of current guards** — already blocked by `min_tp_pct: 3.0` since `|278.8 − 282|/282 = 1.13% < 3%`. Confirmed in test B.
- **Not committed/pushed yet** — local `.git` clean of locks at session end but Linh deploys from PowerShell. Deploy block in `session-6-phase-2.4.md`.
- **Files I did NOT touch** (still uncommitted from prior sessions, Linh should review separately): `state/goal.yaml`, `snapshot.sh`, `state/tao_usdt/trades.jsonl`, `next-session-prompt.md`, `state/btc_usdt/goal.yaml`, deleted `*.tar.gz` artifacts.
- **Watchlist:** Phase 2.5 `fetch_last_closed_pnl` direction-aware pnl_pct fix; Phase 2.3 `fetch_recent_closed_trades` for dashboard merge (pair with 2.5); Phase 2.2 ATR trailing stop (waits for 5–10 calibration trades).

#### Session 6 cont. (later) — Phase 2.5 pnl_pct fix + dashboard $ figures + Ollama context bump + CSV importer
- **Diagnosis from manual reflection trigger**: fallback fired with "Drawdown 557.33% exceeded goal 5.00%" — absurd. Root cause: existing `fetch_last_closed_pnl` formula `(exit - entry) / entry` is direction-blind, so every short was sign-flipped in `trades.jsonl`. Linh confirmed by comparing dashboard vs Bybit CSV export.
- **Bybit reality from CSV (uploaded by Linh)**: 45 closed trades in window, total realized P&L **+$44.41** (winning bot!). TAO 30 trades +$23.58 (50% win, 12/20 shorts won). BTC 5 trades +$10.41 (60%). ETH 7 +$3.02 (57%). SOL 3 +$7.40 (67%). Dashboard had been showing -3.07% all-time because of the sign flip.
- **Local vs Bybit gap**: 45 Bybit trades vs ~30 in local jsonl. 15 trades fired on Bybit but never landed locally (agent restarted between fire and log, or earlier silent log failures). Backfill tool couldn't fetch them because Bybit's `/v5/position/closed-pnl` endpoint has a 7-day query window.
- **Fixes shipped (wave 3)**:
  1. `execution.fetch_last_closed_pnl` rewrite: direction-correct pnl_pct from `closedPnl / cumEntryValue` (matches the backfill convention). Also returns `direction` and `qty` for richer reconcile records.
  2. `tools/recompute_pnl_pct.py` (NEW, ~250 lines): walks each `state/<slug>/trades.jsonl`, recomputes pnl_pct purely from `entry_price`, `exit_price`, `direction` (no Bybit re-fetch needed). Preserves old value as `pnl_pct_old`. Idempotent. Skips backfilled records by default (they're already direction-correct).
  3. `tools/import_bybit_csv.py` (NEW, ~250 lines): parses Bybit's UI-exported CSV, infers direction from sign(P&L) vs sign(exit-entry), dedupes against existing records by (asset, ts_minute, entry_rounded). Flags imports as `csv_imported: true` and `strategy_version: "csv_import"` so reflection can distinguish them.
  4. `reflect.py` LLM prompt shrink: replaced full 25-trade `recent_trades` with compact summaries (`ts, dir, pnl_pct, conf, fired, close, rr`). Trimmed memory_context tail from 3000 → 1500 chars. Expected prompt size drops from ~18K tokens (over Ollama's 4K limit) to well under 4K.
  5. `reflect.py` Ollama context bump: added `options.num_ctx: 8192` to LLM request payload, configurable via `HERMES_LLM_NUM_CTX` env. qwen2.5:3b natively supports 32K; Ollama defaults to 4K unless told otherwise. RAM cost ~1GB extra for KV cache; VPS has 6.7GB free.
  6. `dashboard.py` $ figure additions: new `_pnl_usd(t)` helper resolves $ P&L (prefers Bybit's `closed_pnl_usdt`, falls back to `pnl_pct × entry × qty`). New `_period_pnl_usd` for aggregates. Added $ row to all 4 Running P&L summary cards. Added "P&L $" column to trade history table alongside existing "P&L %".
- **Test coverage**: 4 pnl_pct correctness tests, 6 importer tests (direction inference truth table, row→trade for known short win, real-CSV dry-run for 45 rows, dedup, idempotency, dry-run safety). All green.
- **Rollback of bad v02**: TAO `strategy.yaml` restored from `history/v0000.yaml` (was bumped by fallback acting on flipped pnl_pct). Backed up to `/tmp/rollback-tao-1780037833.yaml`.
- **Ollama is healthy** — not the bottleneck. Issue was prompt size (18K vs 4K limit) and the model truncating then hanging. Fixed by both shrink + num_ctx bump.
- **Not yet deployed.** Combined deploy block coming next. Files changed: execution.py, reflect.py, dashboard.py (local only — no VPS deploy needed), tools/recompute_pnl_pct.py (NEW), tools/import_bybit_csv.py (NEW), data/bybit-closed-pnl-2026-05-29.csv (NEW reference copy).

#### Session 6 wave 4 — Data repair + first successful Hermes reflection (2026-05-29)
**The big win**: first Hermes-driven (LLM, not fallback) strategy mutation in project history. TAO v02 → v03: `indicators[volume_spike].params.min_ratio 1.5 → 2.0` based on actual decision_context (~30 closed trades, 50% win rate, +5.6% realized).

**Bugs found and fixed (in order encountered)**:
1. `fetch_last_closed_pnl` direction-blind formula — Linh confirmed via Bybit CSV (45 trades total +$44.41) that all short trade pnl_pct signs were inverted in trades.jsonl. Fixed: now uses `closedPnl / cumEntryValue`. Plus wrote `tools/recompute_pnl_pct.py` to repair existing records (11 sign-flips: BTC 2, ETH 2, TAO 7).
2. `_max_drawdown` divide-by-near-zero — old formula `(peak - c) / (abs(peak) + 1e-9)` amplified small dips into 80%+ false drawdowns. Fixed to compound wealth-curve `(peak - wealth) / peak`, bounded [0,1]. Sanity-checked: Linh's 24-trade balanced book now reports ~2% drawdown, not 82%.
3. CSV importer (Bybit truth source) was missing on day 1 — wrote `tools/import_bybit_csv.py`, but dedup logic went through three iterations:
   - v1: (asset, ts_minute, entry) — failed because local ts = OPEN, CSV ts = CLOSE (6–8h gap)
   - v2: (asset, entry, exit, qty) — failed because Bybit's avg-entry differs from local fill price by cents
   - v3: (asset, exit, qty) — works, dedupes the 11 real local closed trades exactly
4. Local trades.jsonl polluted by abandoned stubs from prior agent restarts (17 of 25 TAO records had `entry==exit AND abandoned: true`). Wrote `tools/purge_abandoned.py` — removed 20 stubs across all assets. Backups at `*.jsonl.bak-<unix>`.
5. Ollama timed out at 120s on every Hermes call — root cause was first-call cold reload of model with new num_ctx=8192 (added earlier in session). Fixed by bumping `HERMES_LLM_TIMEOUT=300` in .env + pre-warming with one trivial curl. Reflection now completes in 30–60s.
6. LLM dot-notation clobber risk — first Hermes mutation today's payload looked like it used dot notation (`indicators.params.min_ratio`) because rich library silently stripped `[volume_spike]` from the live display. Actual stored changed_variable was the correct bracket form, and `_set_nested` updated volume_spike correctly. Display-only bug. Pushed two defensive guards anyway: reject any `indicators.*` path without brackets, refuse to overwrite a list with a dict. Also escaped brackets in the rich-print to prevent the display confusion recurring.

**Tools shipped this wave** (all in `tools/`):
- `recompute_pnl_pct.py` — direction-correct pnl_pct from entry+exit+direction (preserves old as pnl_pct_old)
- `import_bybit_csv.py` — Bybit CSV → trades.jsonl, infers direction from sign(P&L) vs sign(exit-entry), dedupes by (exit, qty)
- `purge_abandoned.py` — removes entry==exit OR abandoned=true stubs, writes timestamped backup

**Dashboard ($ figures shipped earlier in wave)**: each Running P&L card now shows % above $; trade history has both "P&L %" and "P&L $" columns. Resolved from `closed_pnl_usdt` if present, else computed from `pnl_pct × entry × qty`.

**Strategy mutation chain on TAO**:
- v01 (original) → v02 fallback (drawdown bug, position_size_r 0.8 → 0.85, rolled back) → re-mutated v01 → v02 fallback (drawdown fixed, position_size_r 0.8 → 0.85, KEPT) → v03 Hermes (volume_spike.min_ratio 1.5 → 2.0, KEPT)

**Remaining known issues (defer)**:
- strategy.yaml lacks a top-level `version` field, so archive always uses default "00" → v0000.yaml overwrites itself. Cosmetic; the version IS being read+bumped (just from somewhere else in the file).
- Dashboard merge with Bybit closed-pnl for full audit (Phase 2.3 originally) — partly addressed by CSV import but not by API merge.
- ATR-based trailing stop (Phase 2.2) — wait for more clean trades.
- Bot continues to win small ($5–15 per trade) on ~50% win rate.

**Doctrine added this session (apply going forward)**:
1. **PowerShell -> SSH -> bash quote tax is real.** PowerShell strips embedded `"..."` from single-quoted strings unpredictably, especially when the string contains `(`, `)`, `|`, or nested quotes. For any complex bash command via ssh, use one of:
   - Double-up single quotes: `'sed -i ''s/x/y/'' file'` (the `''` collapses to one `'` in the literal)
   - PowerShell here-string piped to ssh: `@'...'@ | ssh root@host`
   - SCP a `.sh` file to /tmp and execute it remotely
   Never trust quoted output that worked once — it can break on the next invocation depending on the surrounding command.
2. **Always run a real verify after a `cp` rollback.** The `cp v0001.yaml strategy.yaml` failed silently because v0001.yaml didn't exist, and the diagnostic was assumed to confirm it worked. Verify with `grep` for an EXPECTED value (e.g. `grep position_size_r`) and explicitly confirm the value. Don't just check the file parses.
3. **`rich` markup eats bracket-pair strings in console.print.** Always escape `[name]` in printed paths by replacing `[` with `\[` if the variable might contain bracket notation. Hypotheses.jsonl is the truth source, not stdout. Fixed today in run_hermes display.
4. **LLM hypothesis recording must happen BEFORE strategy.yaml is overwritten.** Currently it does (the failed-application path records `error_reason: key_not_found` before falling back). Keep this invariant.
5. **One-step deploy blocks per logical action.** Today's "Step 2: diff + cp" combined both, only the diff was run, downstream tools failed with `ModuleNotFoundError`. Each `cp`, each `git pull`, each restart should be its own labeled code block.
6. **Backups before destructive ops.** purge_abandoned writes `.jsonl.bak-<unix>`; recompute_pnl_pct preserves `pnl_pct_old`; rollback should backup current strategy.yaml to `/tmp/` first. Every tool that mutates a state file should leave a trail.
7. **`_set_nested` clobber guards added.** Rejects dot-notation indicator paths (`indicators.params.X` without `[name]`) and refuses to overwrite an existing list with a dict. Defense against the next time the LLM forgets bracket notation.

#### Session 6 cont. — Audit trail additions + reflect.py None-safety hotfix
- **Triggered by**: first manual `reflect --hermes` for TAO crashed with `TypeError: float() argument must be a string or a real number, not 'NoneType'` at reflect.py:434. Root cause: `t.get("pnl_pct", 0)` returns `None` when the key exists with value `None` (every open trade), not the `0` default. Same bug pattern in `_realised_return` and `_max_drawdown`. Latent for weeks because the IndentationError masked it.
- **None-safety fix**: filtered `pnl_pct is not None` in `_realised_return`, `_max_drawdown`, and the `run_hermes` performance_summary aggregation. Also restructured `run_hermes` to compute `closed = [...]` once and reuse for `winning/losing/win_rate/total_pnl`, and added explicit `closed_trades` + `open_trades` counts to the prompt the LLM sees (more informative).
- **Trade-record audit additions** (essential scope, per Linh): added 4 new fields to every trade record at entry time:
  - `confidence_breakdown` — per-indicator dict `{name: {fired, required, weight, weight_contributed, value, threshold, comparator}}`. Built by `_evaluate_entry` from the new dict-returning `_check_indicator`.
  - `entry_gates` — snapshot of all gate values from strategy.yaml at decision time (min_confidence, min_indicators, max_sl_pct, min_tp_pct, min_rr_ratio, min_profit_usd, risk_per_trade, default_leverage, sl_buffer_pct, strategy_version). New helper `_entry_gates_snapshot(strategy)` in loop.py.
  - `evaluation_summary` — one-line human-readable narrative like "SHORT | fired=['rsi','ema_trend'] | no-fire=['macd','vwap'] | conf=70.00% | FIRE" or with "SKIP: confidence 24% < min 30%".
  - `close_reason` — set at closure: `TP_hit` | `SL_hit` | `manual_or_other` | `abandoned_stale` | `abandoned_no_pnl` | `unknown`. New `_infer_close_reason(exit, tp, sl, tol=0.5%)` helper. Wired in both `_reconcile_open_trades` paths.
- **Hypothesis-record audit additions** (essential scope): added 4 new fields to every hypothesis record in both `--hermes` and `--fallback` modes:
  - `decision_context` — performance_summary block as seen by LLM (closed/open/wins/losses/win_rate/total_pnl/avg_indicators_on_wins/losses) OR rule-based metrics for fallback.
  - `trade_range` — `{count, from_ts, to_ts, trade_ids}` so audits can re-fetch the exact trade window the decision was based on. New `_trade_range(trades)` helper in reflect.py.
  - `applied_successfully` — bool from `_set_nested` return. If False (LLM proposed an unknown key), the failed attempt is STILL written to hypotheses.jsonl with `error_reason: "key_not_found"`, then run_fallback is invoked. Preserves audit trail even when LLM is wrong.
  - `llm_raw_output` — literal string the LLM returned, BEFORE `json.loads`. Critical for debugging malformed-JSON cases. Set to `None` for fallback mode.
- **Test coverage**: 14 audit tests all green (in addition to the 12 from earlier in session 6). Covers `_check_indicator` dict shape, `_evaluate_entry` breakdown + skip-reason summary + fallback path, `_entry_gates_snapshot` completeness, `_infer_close_reason` for TP/SL/manual/unknown, `_trade_range` empty + populated, None-safety regression, source-level checks confirming all 4 audit fields write in both reflection modes.
- **Files changed this sub-session**: `hermes_trading/loop.py` (5 edits: _check_indicator rewrite, _evaluate_entry rewrite, +2 helpers, _reconcile_open_trades, _simulate_paper_trade), `hermes_trading/adapters/execution.py` (2 edits: import + trade record), `hermes_trading/reflect.py` (3 edits: None-safety, _trade_range helper, both hypothesis-write paths).
- **Not yet deployed.** Combined commit + deploy block coming next.

### 2026-05-27 (session 5) — Diagnosis of 0% confidence / no-entry symptom
- **Read-only session.** No Python, YAML, or runtime state modified.
- **Bug 1 (critical, 3 of 4 assets):** `state/eth_usdt/strategy.yaml`, `state/sol_usdt/strategy.yaml`, `state/tao_usdt/strategy.yaml` are in a legacy flat schema (`position_size_r`, `rsi_entry_oversold`, etc.) with NO `indicators:` block and NO `entry:` block. `loop._evaluate_entry` therefore falls into the no-indicators fallback at line 256–261, which is hard-coded to `direction="long"` and `threshold=30`. RSI in 60–75 → `rsi < 30` is False → fires=False, conf=0%. Matches reported symptom exactly.
- **Bug 2 (high, BTC):** `state/btc_usdt/strategy.yaml` v02 still has `rsi.required:true, threshold:30`. Memory.md sessions 3+4 claimed RSI was demoted to optional via `patch_strategies.py`, but the local file disagrees. Hard gate blocks every long entry while RSI > 30. Short leg passes the gate trivially but optional indicators rarely reach the 0.3 confidence threshold in current tape.
- **Bug 3 (low, leverage):** `execution.py:243` only catches `ccxt.AuthenticationError`. Bybit retCode 110043 ("leverage not modified") surfaces as `ccxt.ExchangeError` and propagates as red error spam. Cosmetic — doesn't block trades, but inflates the consecutive-failure counter. Also a latent `NameError` on line 246 if `default_leverage` is absent on a non-Bybit exchange.
- **Recommendation:** apply `patch_strategies.py` to local + VPS (idempotent, YAML-only) to fix bugs 1 & 2. Defer the 7-line `execution.py` leverage wrapper (bug 3) to a separate session so signal-frequency changes can be attributed cleanly to the YAML fix.
- **No code or YAML changes were made.** Memory file updated, diagnosis report `diagnosis-session-5.md` created in project root.

#### Session 5b (same day) — Linh said "do all", applied
- Ran `patch_strategies.py` locally — all 4 strategy.yaml now have direction:both, min_indicators:2, rsi.required:false, default_leverage:5, sl_buffer_pct:0.3, max_sl_pct:5.0.
- Critical follow-on caught: ETH/SOL/TAO had **no `indicators:` block** before, so they would have fallen into loop._evaluate_entry's fallback (returns conf=1.0 every tick on short side once direction=both). Copied BTC's 9-indicator registry into all three via a small yaml-safe Python script.
- Applied 7-line `execution.py` edit: lifted `leverage` out of `try`, added `except ccxt.ExchangeError` that swallows retCode 110043 and re-raises everything else, removed dead `leverage_display` (confirmed unused via grep).
- **Edit-tool corruption:** the leverage edit truncated the last 8 lines of `fetch_last_closed_pnl`. Detected by `py_compile`. Restored via Python script that re-attached the original tail verbatim. AST parse + py_compile both OK afterwards.
- **PyYAML comment loss:** `patch_strategies.py` uses `yaml.dump` which strips human-written comments. ETH/SOL/TAO YAML structural content is preserved but the inline notes were lost. Acceptable trade-off; original files are in `/tmp/{asset}_before.yaml` in the sandbox for reference if needed.
- **BTC default_leverage change:** BTC YAML had `default_leverage:` commented out (let Bybit use 12.5x default). Patch re-set it to 5. Combined with the new idempotent 110043 handler, this is safe — but it IS a behavior change.
- **Not committed/pushed yet** — `.git/index.lock` exists from Windows and the sandbox can't unlink it. Full PowerShell + VPS deploy block written into `diagnosis-session-5.md` (Session 5 — Applied section).
- **Lesson logged:** `Edit` tool is unsafe on files visible via the Windows mount. Always `python -m py_compile` after every Python edit. Prefer Read + Write (full overwrite) over Edit for `.py` files in this project.

#### Session 5c (same day) — Push, deploy, recovery, agent restart
- Push from sandbox blocked (no GitHub creds); Linh pushed `36bfbb3` from PowerShell.
- VPS deploy went WRONG on first attempt: my deploy block in `diagnosis-session-5.md` used incorrect paths (nested `hermes_trading/hermes_trading/state/...`, `venv` instead of `.venv`, `logs/hermes.log` instead of top-level `bot.log`). Linh ran it, `pkill` killed the agent, all `cp` commands failed, `source venv/bin/activate` failed, `nohup` exited 1 → **agent down for ~15 minutes**.
- Read-only diagnostic block mapped the actual VPS layout (see "VPS layout" section above). Confirmed:
  - `/opt/trading/hermes_trading/hermes_trading/adapters/execution.py` on the VPS was already byte-identical to my session 5 version (someone had applied the 110043 handler locally before; the `M` in earlier `git status` was for that).
  - All 4 `state/<slug>/strategy.yaml` files in running dir still had the pre-session-5 shapes → critical to copy.
  - Per-asset `trades.jsonl` files were all 0 lines → confirms signal generation, not execution, was the root cause.
- Recovery deploy: backup → copy 4 strategy.yamls → restart with correct paths. Agent up as PID 1290662, all 4 workers booted in live mode at correct state dirs.
- **Backup of pre-deploy strategy.yamls at `/tmp/hermes-backup-<STAMP>/` on VPS.** Will persist until VPS reboot or `/tmp` cleanup. Should be SCP'd to local for permanence if Hermes-evolved values matter.
- **Awaiting first 15m tick** to confirm `dir=both · conf=NN%` lines appear and no new `110043` tracebacks.

### Lessons added to deploy doctrine (apply going forward)
1. **`logs/hermes.log` does not exist on this VPS.** Top-level `bot.log` is the log. Update all deploy blocks.
2. **`venv` does not exist; it's `.venv`.** Update all deploy blocks.
3. **State dirs are siblings of the package, not nested inside it.** Path is `/opt/trading/hermes_trading/state/<slug>/strategy.yaml`. The "nested package inside package dir" line in earlier memory was misleading — the package IS nested (`/opt/trading/hermes_trading/hermes_trading/`), but state is not.
4. **Always run a `diff` between SOURCE and RUNNING before any `cp`** — execution.py was already up-to-date on this VPS, so the `cp` would have been a no-op but the discipline matters.
5. **Always backup state files before overwriting** — `mkdir -p /tmp/hermes-backup-$(date +%Y%m%d_%H%M%S)/` then `cp` the running yamls there first.
6. **Prefer single-line semicolon-separated bash blocks for SSH paste.** Multi-line PowerShell-style blocks misbehave at `>>` continuation prompts and break in PowerShell when Linh pastes into the wrong window.

### 2026-05-25 (session 4) — Phase 1 SMC implementation
- **Phase 1 SMC: structural SL/TP** — `execution.py` now derives SL from `support_1h4h` (long) or `resistance_1h4h` (short) with a `sl_buffer_pct` (0.3%) buffer. TP set at nearest structural resistance/support. Both fall back to fixed `stop_loss_pct` when no structural level is available.
- **Risk-based position sizing** — replaced fixed 20% of balance with `(balance × risk_per_trade) / sl_dist_pct`. `risk_per_trade=10%`, capped at `MAX_POSITION_USD=$500` env var.
- **max_sl_pct guard** — if structural SL is >5% from entry, trade is skipped (raises ValueError). loop.py catches this and logs "Entry skipped — structural SL too wide".
- **Fixed leverage** — `default_leverage=5` replaces RSI-scaled 3–15x. Simpler, more consistent.
- **R:R ratio** — stored in trade records (`rr_ratio`); dashboard Live Positions table shows it in colour (green ≥1.5R, amber <1.5R). Computed from structural TP/SL distances.
- **Paper sim updated** — `_simulate_paper_trade` uses `_structural_sl_tp` for consistent paper/live behaviour.
- **reflect.py** — added `risk_per_trade`, `sl_buffer_pct`, `max_sl_pct`, `default_leverage` to AI tunable vars.
- **risk_per_trade set to 10%** per Linh's request (was 1%).
- **Deploy from Windows PowerShell**:
  ```
  cd C:\Users\nghil\Projects\Hermes\Hermes-Trading
  Remove-Item .git\index.lock -Force -ErrorAction SilentlyContinue
  git add hermes_trading/adapters/execution.py hermes_trading/loop.py hermes_trading/run.py hermes_trading/reflect.py dashboard.py patch_strategies.py memory.md
  git commit -m "feat: Phase 1 SMC — structural SL/TP, risk-based sizing (10%), fixed leverage, R:R in dashboard"
  git push origin master
  ```
- **Then on VPS**:
  ```
  cd /opt/trading/hermes-trading && git pull
  cp hermes_trading/adapters/execution.py /opt/trading/hermes_trading/hermes_trading/adapters/execution.py
  cp hermes_trading/loop.py /opt/trading/hermes_trading/hermes_trading/loop.py
  cp hermes_trading/run.py /opt/trading/hermes_trading/hermes_trading/run.py
  cp hermes_trading/reflect.py /opt/trading/hermes_trading/hermes_trading/reflect.py
  cp patch_strategies.py /opt/trading/hermes_trading/hermes_trading/patch_strategies.py
  cd /opt/trading/hermes_trading/hermes_trading
  python3 patch_strategies.py
  pkill -f hermes_trading.run
  cd /opt/trading/hermes_trading && source venv/bin/activate
  nohup python3 -m hermes_trading.run >> logs/hermes.log 2>&1 &
  ```

### 2026-05-24 (session 3)
- **Diagnosed agent DOWN**: goal.yaml line 31 had `%` instead of `#` causing yaml.ParserError on startup. Fixed.
- **Open trade reconciliation bug fixed**: _reconcile_open_trades previously only updated the most recent open trade. Old ghost records (TAO 05-23, ETH 05-23) stayed open forever. Now abandons ALL stale open records (pnl_pct=0, abandoned=True), keeping only the most recent (which matches the live Bybit position).
- **direction:both added**: loop._evaluate_entry now takes force_direction param. run() evaluates long AND short every tick when strategy says "both", picks the higher-confidence side. Ambiguous signals (< 0.1 confidence diff) are skipped.
- **min_indicators added**: entry.min_indicators (default 2) requires at least N optional indicators to fire. Replaces the single required RSI gate.
- **RSI demoted to optional**: patch_strategies.py updates all 4 existing VPS strategy.yaml files to set required:False on RSI, direction:both, min_indicators:2. Run once on VPS after deploy.
- **execution.py**: direction now read from entry_detail (resolved per-tick) not hardcoded from strategy, so direction:both correctly places long or short.
- **Dashboard - Live Positions section**: new table showing open trades with unrealised P&L vs current price, SL/TP levels.
- **Dashboard - Agent Activity**: replaced "Recent log" (showing crash tracebacks) with filtered event feed showing only trades, reflections, errors. "No entry" spam filtered out.
- **Dashboard - trade history**: abandoned trades now show "abandoned" label in italic; conf column replaces RSI column (RSI was always "—" since field renamed).
- **Deploy from Windows**:
  ```
  cd C:\Users\nghil\Projects\Hermes\Hermes-Trading
  git add hermes_trading/loop.py hermes_trading/reflect.py hermes_trading/run.py hermes_trading/adapters/execution.py dashboard.py state/goal.yaml patch_strategies.py memory.md
  git commit -m "fix: goal.yaml; reconcile stale open trades; direction:both; min_indicators; dashboard live positions"
  git push origin master
  ```
- **Then on VPS**:
  ```
  cd /opt/trading/hermes-trading && git pull
  cp hermes_trading/loop.py /opt/trading/hermes_trading/hermes_trading/loop.py
  cp hermes_trading/reflect.py /opt/trading/hermes_trading/hermes_trading/reflect.py
  cp hermes_trading/run.py /opt/trading/hermes_trading/hermes_trading/run.py
  cp hermes_trading/adapters/execution.py /opt/trading/hermes_trading/hermes_trading/adapters/execution.py
  cp state/goal.yaml /opt/trading/hermes_trading/hermes_trading/state/goal.yaml
  cp patch_strategies.py /opt/trading/hermes_trading/hermes_trading/patch_strategies.py
  cd /opt/trading/hermes_trading/hermes_trading
  python3 patch_strategies.py
  pkill -f hermes_trading.run
  cd /opt/trading/hermes_trading && source venv/bin/activate
  nohup python3 -m hermes_trading.run >> logs/hermes.log 2>&1 &
  ```

### 2026-05-24 (session 2)
- Identified critical bug: loop.py called subprocess.run(["python", ...]) — `python` not in PATH on VPS, so all reflections silently failed. Fixed to sys.executable.
- Tightened run_hermes() LLM prompt: replaced inline variable list with newline-delimited list, added explicit "DO NOT use dot notation" rule, added concrete one-shot JSON example showing indicators[name].params.X format.
- Created setup_github_ssh.sh: generates ~/.ssh/id_ed25519_hermes, configures ~/.ssh/config, converts remote from HTTPS to SSH, prints public key for GitHub. Run once on VPS, then add key to GitHub to enable snapshot.sh push.
- Updated memory.md and key decisions table.
- git push blocked by stale index.lock in sandbox (Windows mount) — deploy instructions below.
- **Deploy from Windows PowerShell**:
  ```
  cd C:\Users\nghil\Projects\Hermes\Hermes-Trading
  git add hermes_trading/loop.py hermes_trading/reflect.py setup_github_ssh.sh memory.md
  git commit -m "fix: sys.executable in reflection subprocess; tighten LLM bracket notation; SSH setup"
  git push origin master
  ```
- **Then on VPS**:
  ```
  cd /opt/trading/hermes-trading && git pull
  cp hermes_trading/loop.py /opt/trading/hermes_trading/hermes_trading/loop.py
  cp hermes_trading/reflect.py /opt/trading/hermes_trading/hermes_trading/reflect.py
  cp setup_github_ssh.sh /opt/trading/hermes_trading/setup_github_ssh.sh
  # Restart agent:
  pkill -f hermes_trading.run
  cd /opt/trading/hermes_trading
  source venv/bin/activate
  nohup python3 -m hermes_trading.run >> logs/hermes.log 2>&1 &
  # Then set up GitHub SSH:
  bash /opt/trading/hermes_trading/setup_github_ssh.sh
  # Copy the printed key → GitHub Settings → SSH keys → New key (title: hermes-vps)
  # Verify: ssh -T git@github.com
  # Test snapshot: bash /opt/trading/hermes_trading/snapshot.sh
  ```

### 2026-05-24
- Read full codebase: run.py, loop.py, reflect.py, dashboard.py, adapters
- Found 3 reflect.py bugs: missing --state-dir, module-level globals, broken indicator key parsing
- Fixed reflect.py: added --state-dir arg, refactored all paths to use state_dir, fixed _set_nested/_get_nested for indicators[name].field notation, added win_rate to hypothesis, memory.md update on every reflection
- Found dashboard bugs: homebrew YAML parser, wrong goal key names, no win rate, no indicator weights panel
- Fixed dashboard.py: proper yaml.safe_load, correct nested goal parsing, win rate + indicator weights panel per asset, last-known-state fallback on SSH error, expanded log tail to 25 lines
- Created memory.md (this file) and state/memory.md template
- Pushed to GitHub, user pulled on VPS and restarted agent with venv activated
- All 4 workers confirmed live: BTC/USDT, ETH/USDT, SOL/USDT, TAO/USDT in live mode
- Enriched trade records: added indicators_snapshot (22 keys), indicators_fired, confidence_at_entry to loop.py and execution.py
- Built snapshot.sh: nightly git worktree commit of evolved strategies to state-snapshots branch
- Wired up Ollama local LLM: _call_llm() in reflect.py uses urllib.request to POST to localhost:11434
- Installed Ollama on VPS, pulled qwen2.5:3b (1.9GB), added HERMES_LLM_* env vars
- Verified first AI reflection: v02 -> v03, changed indicators.params.min_ratio 1.5 -> 3.0
- Initialised fallback reflection for eth/sol/tao: all at v03
- Fixed state/goal.yaml YAML parse error (missing # on line 1)
